interface:
  display_name: "Genai Incidents"
  short_description: "Repo-specific patterns and workflows for genai_incidents"
  default_prompt: "Use the genai_incidents repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true