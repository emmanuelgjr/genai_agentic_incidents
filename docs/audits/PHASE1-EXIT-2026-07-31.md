# Phase 1 — "Honest" exit checklist — RE-ISSUE, 2026-07-31

> **DO NOT REGENERATE THIS FILE'S FIGURES IN PLACE.** This is a dated record
> against `main` @ `a54e5ce7`. Per working agreement 4 (specs and audits are
> records, not sources), when this checklist is next overtaken it is
> **superseded by a new dated file**, not rewritten. `docs/audits/PHASE1-EXIT-2026-07-30.md`
> is the prior record and is **not edited by this file** — this file supersedes
> it. (That file itself has five commits, the latest `69fd0da4` an in-place
> tense correction of its own E23 bullet — a fact about *that* file's history,
> not a claim this document makes about it.)

**Prepared 2026-07-31, against `main` @ `a54e5ce7`** (also the tip of
`ws0/phase1-exit-checklist`, which carries no code changes — this file only).
Every figure below was **re-derived directly from this tree by this author**,
not transcribed from the board, from `docs/audits/PHASE1-EXIT-2026-07-30.md`,
or from `docs/audits/PHASE1-EXIT-measurements-2026-07-31.md`. Where the
foreman's measurement file is cited, it is cited as a **prior independent
measurement being cross-checked**, and every figure in it was re-run here
against the live tree, not copied.

## Evidence classes

- **[R] Re-derived** — measured *now*, from this tree, with the command or
  file:line shown below. Anyone can reproduce it.
- **[A] Attested** — the deliverable's artifacts are present and verifiable,
  and a gate PASS is recorded contemporaneously on `PROGRESS.md`, but the
  reviewer's verdict text itself exists only in session transcripts, not in
  the repo. **The artifact is evidence; the verdict is testimony**
  (`CLAUDE.md` working agreement 5).

**Hard rule, restated because it is the whole point of this file: no
criterion asserting a measurable property of the repo is marked met on [A]
alone.** Where a criterion asserts a property of the repo, that property is
measured below, command shown. [A] is reserved for testimony — "a gate
returned PASS" — which cannot itself be re-derived from the repo.

## Corpus and version state, re-derived here

| Fact | Value | Command / method |
|---|---:|---|
| `data/incidents.json` entries | **13,060** | `len(json.load(open('data/incidents.json'))['incidents'])` |
| `data/id_deprecations.json` tombstones | **1,051** | `len(json.load(open('data/id_deprecations.json'))['deprecations'])` |
| `data/stats.json` / `pyproject.toml` / `CITATION.cff` version | **2.8.0**, all three, consistent | `grep -n version pyproject.toml CITATION.cff`; `data/stats.json:4` |
| `.zenodo.json` version | **file has no version key** — `.zenodo.json` exists (1229 bytes, `2026-07-17`) but does not carry a `version` field at all. Owner: WS6 (release/deposit mechanics, `MASTER_IMPROVEMENT_PLAN.md:271`) | `grep -in version .zenodo.json` → no match |
| Latest tag | **`v2.8.0`** — no `v2.9.0` tag exists | `git tag -l --sort=-v:refname \| head -1` |
| Test suite | **314 passed** | `python -m pytest -q` |
| Doc-surface drift (invariant 6) | **exit 0**, "clean: 5 doc surfaces match data/stats.json, no unmarked hardcoded totals" | `python scripts/check_stats_drift.py` |
| Untracked strays | **none** | `git status --porcelain --untracked-files=all` → empty |

**The release is NOT cut.** `docs/releases/v2.9.0.md` is a draft (self-labeled
"DRAFT — not released" in its own header); `CHANGELOG.md`'s `[Unreleased]`
section carries the same draft pointer and states "Not cut — version, date,
and final entry count are the maintainer's call." Nothing below should be
read as claiming otherwise.

**`__init__.py`'s `VERSION` constant is stale and exported.**
`src/genai_incidents/__init__.py:39` reads `VERSION = "2.0.0"` and `VERSION`
is in `__all__` (`:29`), disagreeing with every other version surface above.
Not a Phase-1 exit criterion (WS6-T1 is Phase-2/-ongoing scope), noted here
because a reader asking "what version am I running" via the Python API today
gets a wrong answer. Owner: WS6-T1.

## The ten criteria (as amended by D20, D21, D22)

| # | Criterion (verbatim from `MASTER_IMPROVEMENT_PLAN.md`, as amended) | Evidence | Status |
|---|---|---|---|
| 1 | **WS0-T2 done** | [A] `PROGRESS.md:36` — done, PASS red-reviewer re-gate 2026-07-17, merged per D7. [R] re-derived here: the three licensing surfaces the task delivers are present and non-empty — `LICENSE` (1,093 bytes, MIT legal code), `LICENSE-DATA` (19,053 bytes, CC BY 4.0 legal code), `NOTICE-DATA` (12,015 bytes) — plus `.reuse/dep5` (5,197 bytes) cross-referencing them | ✅ MET |
| 2 | **WS0-T5 done** | [A] `PROGRESS.md:37` — done, PASS red-reviewer re-gate 2026-07-17. [R] re-derived here: a glob-scoped search (`*.md`, `*.html`, `*.cff`, `NOTICE-DATA`) for "single source of truth", excluding `docs/audits/`, `PROGRESS.md`, `MASTER_IMPROVEMENT_PLAN.md`, `CLAUDE.md`, `.claude/` (all of which define or quote the banned-phrase rule itself): **one hit**, `docs/incidents/2026.md:99270`, inside a *generated incident record* quoting an upstream security advisory's own "Fix" text ("...use the path param as a single source of truth") — not a project claim about the dataset. **Zero project-voice occurrences within the searched globs.** | ✅ MET |
| 3 | **WS0-T6 done** | [A] `PROGRESS.md:39` — done, PASS red-reviewer re-gate 2026-07-17, merged per D7. [R] re-derived here: `README.md:320` carries a "How to cite" section (aggregate → dataset/DOI; individual incident → its primary source); `CITATION.cff:3` carries a `message:` field | ✅ MET |
| 4 | **WS6-T2 done** | [A] `PROGRESS.md:38` — done, PASS red-reviewer 2026-07-17, invariant 6 activated. [R] re-derived here: `python scripts/check_stats_drift.py` → exit 0, "clean: 5 doc surfaces match" | ✅ MET |
| 5 | **WS3-T5 (decision made) done** | [A] `PROGRESS.md:40` — done, decision made: E4 resolved by D13 (Option B, padding-agnostic parsing commitment). [R] re-derived here: the row text reads *"Status corrected 2026-07-30: this cell previously read '(draft; E4 decision now with user)', which contradicted D13 … Found by the Phase-1 exit checklist as FLAG 1; the checklist's report of the contradiction stays as the record of what it found."* — a dated correction preserving the original text, not a silent rewrite. See **FLAG 1** below. | ✅ MET |
| 6 | **WS0-T1 zero blank rows — every row resolved or carrying pending-outreach status with dates** | [R] re-derived here, direct parse of `docs/SOURCE_LICENSES.md`'s two-column tables: **135 rows scanned, 0 blank cells.** `(d) UNKNOWN` (exact token) occurs **3** times (`docs/SOURCE_LICENSES.md:128`, `:233`, `:344`): line 344 (and its companion at `:128`) is the one genuinely still-open source (MIT AIRI Navigator — its own license is CC BY 4.0, only the AIID-share-alike transitivity question remains, outreach sent 2026-07-27/29); line 233 is inside a resolved CSET row's *conditional tripwire* text ("this row REOPENS as (d) UNKNOWN" if scope ever expands) — a resolution clause, not an open unknown. | ✅ MET — ⚠ see **FLAG 2** |
| 7 | **WS5-T2a outreach sent** | [R] re-derived here, `docs/outreach/annotator-recruitment.md:3` — "SENT to Mayur021 2026-07-27"; `PROGRESS.md:41` — done, PASS red-reviewer re-gate after bounce #1 | ✅ MET |
| 8 | **WS0-T3 reduction complete for sources already confirmed non-redistributable (AIAAIC now; others as their WS0-T1 rows resolve)** | [R] re-derived here, both populations, direct scan of `data/incidents.json`: **AIAAIC** — 1,517 rows carry `content_license` (all `{"source": "aiaaic", "license": "CC-BY-SA-4.0", ...}`). **OECD** — 3,829 rows carry an `OECD-AIM-*` source_id; of those, **3,667** carry a `description` matching the structural template (`"Tracked by the OECD AI Incidents and Hazards Monitor (AIM) as ..."`, built by `build_description()` in `scripts/ingest_oecd_aim.py`), **162** do not (another source won the merge and supplied a different description — mechanism confirmed by reading `scripts/merge_and_dedupe.py:1873`'s `merge_into()`, which never touches `title` or `description`). **0** of 3,829 carry a `content_license` marker (a deliberate posture, not a gap — see criterion 8's note and E23). All four figures independently reproduce the values in `docs/SOURCE_LICENSES.md` §1.5/§3 and `NOTICE-DATA:170-194`. **The four merges that closed this since 2026-07-30 are verified ancestors of `HEAD`:** `7365aee7` (E21 §5 reduction, `git merge-base --is-ancestor 7365aee7 HEAD` → yes), `6f06eaeb` (E21 §5.3 attribution, ancestor confirmed), `e07852fc` (licensing-surfaces completion, ancestor confirmed), `a54e5ce7` (v2.9.0 pre-release packet = current `HEAD`). `PROGRESS.md:25` records the E21-reduction gate: PASS, first gate, 0 defects, 8 advisories, all routed. | ✅ **MET — SETTLED, not pending.** The 2026-07-30 record's "NOT MET" reflected the state before these four merges; this file re-derives the *current* tree and finds the reduction shipped, verified, and merged to `main`. |
| 9 | **WS0-T4 done** *(D21, `MASTER_IMPROVEMENT_PLAN.md:28`, verbatim: "clause 1, the code property, gates the task; clause 2, outreach, tracks separately to completion")* | **Clause 1** [R] re-derived here: `ingest/common.py` exists; `scripts/ingest_utils.py` is gone; `docs/INGESTION_CONDUCT.md` exists; `tests/test_network_chokepoint.py` exists; `MASTER_IMPROVEMENT_PLAN.md:45` (invariant 5) carries the D22-amended HTTP(S)-plus-register text, Active-from "WS0-T4 done". [A] `PROGRESS.md:49` — clause 1 done, merged `8b1e8888`, invariant 5 active, gated PASS after 1 bounce. **Clause 2** [R] re-derived here from `docs/outreach/README.md` and each outreach file's own status line: AIAAIC SENT 2026-07-27 (+correction 2026-07-29), AIID SENT 2026-07-27, MIT AIRI SENT 2026-07-27 (+2026-07-29 export request); **OECD AIM — `docs/outreach/oecd-aim-terms.md:3-4` reads "DRAFTED (2026-07-30) — gated PASS … not yet sent."** Recipient `ai@oecd.org` per `docs/outreach/README.md:21`, marked unconfirmed as to desk. | **Clause 1 ✅ MET** (invariant 5 ACTIVE, re-derived). **Clause 2 ⚠ OPEN — 3 of 4 sent, NOT 4 of 4.** See **FLAG 3**. |
| 10 | **WS4-T9 done — met only when every dead source has EITHER a remediation implemented OR a hold carrying a DECISION DATE** | [R] re-derived here, direct read of `data/source_freshness.json`: 4 tracked sources; 1 `stale` (`airi_navigator`, `last_success: 2026-05-31`, `stale_since: 2026-06-07`); its entry carries `"hold": {"decision": "D8", "until": "2026-08-28", ...}` — a machine-readable decision date. 1,380 rows carry a `source_freshness` field (`sum(1 for i in incidents if 'source_freshness' in i)`). | ✅ MET — ⚠ see **FLAG 5** |

## The five technicality flags — current state, each re-verified here

**FLAG 1 — WS3-T5's board row contradicted D13.** **FIXED, verified.**
`PROGRESS.md:40` now reads "done — decision MADE: E4 resolved by D13 …
*Status corrected 2026-07-30: this cell previously read '(draft; E4 decision
now with user)' … Found by the Phase-1 exit checklist as FLAG 1 …*" — a
dated correction that preserves what the row previously said rather than
silently rewriting it, consistent with working agreement 4.

**FLAG 2 — criterion 6 was satisfied on false statements until 2026-07-30
(E20).** **This is a statement about history and stays disclosed, not
"resolved."** Two of the three then-pending `docs/SOURCE_LICENSES.md` rows
carried an `Outreach date` cell claiming a draft existed ("2026-07-15
(drafted; not yet sent)") for outreach that had never existed on any branch.
Criterion 6 is genuinely met **today** (re-derived above: 0 blank cells, 135
rows scanned); it was not honestly met on any date the board would have
claimed before 2026-07-30. No further action changes this — it is a fact
about the criterion's history, and it stays in the record as one.

**FLAG 3 — criterion 9's clause 2 is 3-of-4, not 4-of-4. OPEN — user
action.** OECD is drafted (`2026-07-30`), gated PASS, **not sent**. Proposed
recipient `ai@oecd.org`, unconfirmed as to desk (`docs/outreach/oecd-aim-terms.md:23`:
"**To (UNCONFIRMED):** `ai@oecd.org`. Not found by this draft's own first-pass
check … surfaced one hop away, at `https://oecd.ai/en/contact`").
Per D21 this does **not** gate invariant 5, which is a property of the code
and is independently active (clause 1, above). But the criterion as written
in the plan says outreach "drafted, sent, and logged with dates," and OECD
is not sent. **Reported as open, not rounded up to 4 of 4** — this project
has twice recorded a criterion satisfied by a date attached to a draft that
did not exist (E18, E20); rounding 3-of-4 up here would be a third instance
of the same failure shape. **This is the only remaining item not in the
project's hands** — it closes when the user sends the OECD email and logs
the date.

**FLAG 4 — `CHANGELOG.md`'s "Static TAXII 2.1 endpoint," the last
banned-phrase instance, sits under a past-release heading. Fork reported, not
resolved.** Re-derived here: the phrase is at `CHANGELOG.md:241`
(`grep -n "Static TAXII 2.1 endpoint" CHANGELOG.md`), under the `## [2.5.0] —
2026-06-11` heading — **a past release, not a live surface.** (The line
number has moved twice since the 2026-07-30 checklist cited `:137` and the
2026-07-31 measurement file cited `:179` — both times because new entries
were prepended to `[Unreleased]` above it; the text itself is byte-identical
across all three citations. Cite by heading, `[2.5.0]`, not by line number,
for anything durable.) Every live surface (`README.md`, site, HF card,
`docs/taxii2/` itself) uses the corrected phrasing ("TAXII-compatible
discovery document" / "read-only static mirror"). **The fork:** working
agreement 4 says a dated record of what was claimed *then* is superseded
with a note, never rewritten in place; WS6-T4's own acceptance criterion is
phrased as an in-place correction ("no surface says 'TAXII endpoint'"). Those
two rules point opposite ways on this one line. **Not resolved here —
flagged for a decision before WS6-T4 is dispatched, as the foreman's
2026-07-31 measurement file already recommended.**

**FLAG 5 — criterion 10 is met by a date that did not exist for 12 days.**
D8 was decided 2026-07-17 and keyed its clock to sending AIRI Draft 4; that
draft existed in no commit on any branch until it was written, gated and
sent 2026-07-29 (E18). This is a statement about the criterion's history,
not a present defect — re-derived above, the hold now carries a genuine,
machine-readable decision date (`{"decision":"D8","until":"2026-08-28"}`,
`data/source_freshness.json:26`), and stays disclosed as the reason
D20(b)(ii) sharpened this criterion's wording in the first place.

## The two banked consistency items

**`dep5:44` — the maker-non-qualification grammar. RESOLVED, verified here.**
`.reuse/dep5:42-46` reads: "AIID's maker is US-situated, so U.S. Circular 33
excludes the retained title from copyright, and that same US situs means
AIID's maker **fails both the UK and EU sui generis database-right
qualification tests**, so no such right subsists in AIID's collections."
`NOTICE-DATA:114-123` states the same framing in its own words: "...that situs
closes both routes CC BY-SA offers to share-alike: U.S. Copyright Office
Circular 33 categorically excludes titles and short phrases from copyright,
so the retained AIID title carries no protectable expression; and a
**U.S.-situated maker fails both the UK and EU sui generis database-right
qualification tests**, so no such right subsists in AIID's collections to
begin with." The two files attribute non-subsistence to the *maker's*
non-qualification, in matching (not merely similar) wording, in both places.

**REUSE lint — command shown, no fraction cited, deliberately.** This
document is itself a file `reuse lint` counts, so any `N / M` it prints
measures a tree that does not yet contain this document's own commit — the
fraction is stale from the instant this file is committed, in a way that
`13,060`, `1,517`, `314`, `135` and every other figure above is not (adding
this markdown file does not move any of those). A `python -m reuse lint`
result quoted here would also be flatly wrong for whoever runs the
"Verification recipe" command below on this committed file, since the tree
they stand in differs from the tree that produced whatever number was typed
here. Per the ruling on this point: state the command and the qualitative,
tree-stable finding, not the count.

```
$ python -m reuse lint
```

**Qualitative finding, stable across the file-count churn:** non-compliance
is concentrated in two classes, both long-known and unchanged in kind since
the first measurement of this figure — (1) generated/vendored data snapshots
carrying no REUSE tag (`ingest/*_incidents.json`, `legacy/incidents.json`,
`src/genai_incidents/data/incidents.min.json`), and (2) two process/schema
files (`PROGRESS.md`, `src/genai_incidents/schema/incident.schema.json`).
The project is **not REUSE-compliant** today by this tool's own verdict.
Re-run the command above at whatever point the fraction is actually needed;
do not carry a number from this file forward.

## What Phase-1 exit does and does not assert

**Nine of ten criteria are met, all re-derived directly against `main` @
`a54e5ce7` in this file.** Criterion 8 — the last substantive blocker as of
the 2026-07-30 record — is now closed: the OECD reduction shipped, gated,
and merged. **Criterion 9's clause 2 is open** (FLAG 3): 3 of 4 outreach
emails are sent; OECD is drafted and gated but not sent.

**This does not mean "Phase 1 exits."** The plan's Phase-1 exit criteria
list WS0-T4 done as a single criterion; D21 split it into two clauses
precisely so that clause 1 (the code property, which gates invariant 5)
would not be held hostage to clause 2 (an outreach send that is not in this
project's hands to schedule) — but the split does not mean clause 2 stopped
being part of the criterion. Read literally, **the plan's WS0-T4 criterion
is not yet fully met while clause 2 is open**, and this checklist reports it
that way rather than treating the D21 split as a silent downgrade of the
criterion. What has changed since 2026-07-30 is that Phase 1 now has
exactly **one** open item, it is **not** blocked on further engineering or
gating work, and it is a **single user action** (send the OECD email, log
the date) away from every criterion in the ten-row table above reading MET.
Everything else this checklist can measure, it has measured, and it holds.

This checklist does not claim the [A]-class rows were independently
re-verified by this document beyond their artifacts (WS0-T2/T5/T6/WS6-T2/
WS5-T2a's PASS verdicts are testimony recorded contemporaneously on
`PROGRESS.md`, per working agreement 5) — but every criterion that asserts a
measurable property of the repo (1, 2, 3, 4, 6, 7, 8, 9-clause-1, 10, plus
the corpus/version/test-suite table at the top) is marked [R] and was re-run
against the live tree by this author, not inherited from any prior document.

## Verification recipe for the reviewer

```
git symbolic-ref -q HEAD                                   # branch check
git status --porcelain --untracked-files=all               # expect empty
python -m pytest -q                                        # expect 314 passed
python scripts/check_stats_drift.py                        # expect exit 0
python -m reuse lint                                        # non-compliant; count is NOT reproducible from this file (this file's own presence shifts it) -- see "banked consistency items"
grep -n "Static TAXII 2.1 endpoint" CHANGELOG.md            # expect one hit, under [2.5.0]
grep -c "(d) UNKNOWN" docs/SOURCE_LICENSES.md               # expect 3
git merge-base --is-ancestor 7365aee7 HEAD && echo yes      # criterion 8, merge 1
git merge-base --is-ancestor 6f06eaeb HEAD && echo yes      # criterion 8, merge 2
git merge-base --is-ancestor e07852fc HEAD && echo yes      # criterion 8, merge 3
python -c "
import json
d = json.load(open('data/incidents.json', encoding='utf-8'))
inc = d['incidents']
print('entries', len(inc))
cl = [i for i in inc if i.get('content_license')]
print('content_license', len(cl), {i['content_license']['license'] for i in cl})
oecd = [i for i in inc if any(s.startswith('OECD-AIM-') for s in i.get('source_ids', []))]
reduced = [i for i in oecd if (i.get('description') or '').startswith(
    'Tracked by the OECD AI Incidents and Hazards Monitor (AIM) as')]
print('OECD rows', len(oecd), 'reduced', len(reduced), 'residue', len(oecd) - len(reduced))
"
```
