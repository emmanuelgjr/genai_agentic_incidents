# Phase 1 — "Honest": exit checklist

**Prepared by the foreman, 2026-07-30, against `main` @ `a8b67167`.** Per D19 the
foreman brings this checklist when the Phase-1 tail is done; per the user's
2026-07-29 format ruling it is a committed file, one row per exit criterion **as
amended by D20**, criterion → evidence → status, with evidence **re-derived, not
recalled**, and any criterion satisfied on a **technicality** actively flagged
rather than left for the reader to find.

## How to read the evidence column — and why this distinction is in the document

Two evidence classes, marked per row. The distinction is not pedantry: it is the
honest answer to "re-derived, not recalled", and it was prompted by a gate's own
statement of its limits during the D8 re-verify, which is worth quoting because
this document inherits the same constraint:

> *"I can verify that the repo records 'OECD task PASS' and that the citation
> resolves to it. I **cannot** independently verify the gate's verdict text —
> that exists only in a session transcript, not in the repo. … Verified to a
> contemporaneous foreman record, not to the gate's own words."*

- **[R] Re-derived** — measured from the repo on 2026-07-30, with the command or
  file:line shown. Anyone can reproduce it.
- **[A] Attested** — the deliverable's artifacts are present and verifiable, and
  a gate PASS is recorded contemporaneously on `PROGRESS.md`, but the reviewer's
  verdict text itself exists only in session transcripts. **The artifact is
  evidence; the verdict is testimony.** Rows marked [A] are not weaker claims
  about whether the work exists — they are honest about what kind of record
  supports "it was independently gated".

**No row below is marked ✅ on [A] evidence alone where the criterion asserts a
measurable property.** Where a criterion asserts a *property of the repo*, that
property is re-derived.

## Corpus and version state, re-derived

| Fact | Value | Source |
|---|---|---|
| `data/stats.json` `incident_count` | **13,119** | [R] `data/stats.json` |
| `data/incidents.json` entries | **13,119** | [R] parsed |
| `landmark_count` | 1,905 | [R] `data/stats.json` |
| Version strings | **2.8.0** in `pyproject.toml`, `CITATION.cff`, `.zenodo.json`, `data/stats.json`; latest tag `v2.8.0` | [R] |
| Commits since `v2.8.0` | **220 total**, of which **6 changed the corpus** | [R] `git rev-list --count v2.8.0..HEAD`; corpus-affecting count per docs-warden 2026-07-30 |
| `CHANGELOG.md` newest entry | **`[2.8.0]`** (2026-07-13, at 12,986 entries) | [R] |
| Doc-surface drift | **clean** — `check_stats_drift.py`: 5 surfaces match, no unmarked hardcoded totals | [R] exit 0 |
| Test suite | **283 passed** | [R] `pytest -q` |
| `validate.py` | exit 0 — 13,119/13,119 valid, 0 errors | [R] |

## The criteria

| # | Criterion (as amended by D20) | Evidence | Status |
|---|---|---|---|
| 1 | **WS0-T2 done** | [A] board row `done`; licensing surfaces present and internally consistent per docs-warden 2026-07-30 (`LICENSE`, `LICENSE-DATA`, `NOTICE-DATA`, `.reuse/dep5` cross-checked) | ✅ MET |
| 2 | **WS0-T5 done** | [A] board row `done`. [R] the overclaim rule holds: **zero live "single source of truth" hits** repo-wide except the banned-phrase rule's own definition (docs-warden 2026-07-30) | ✅ MET — ⚠ see FLAG 4 |
| 3 | **WS0-T6 done** | [A] board row `done` | ✅ MET |
| 4 | **WS6-T2 done** | [A] board row `done`. [R] its mechanism is live and green: `check_stats_drift.py` clean across 5 marker-templated surfaces, and main's post-#97 drift-red state is confirmed **resolved** | ✅ MET |
| 5 | **WS3-T5 (decision made) done** | [A] board row `done`; the decision exists as **D13** ("E4 DECIDED: Option B — padding-agnostic parsing commitment") | ✅ MET — ⚠ **see FLAG 1**: the board row's own parenthetical still reads "(draft; E4 decision now with user)", which contradicts D13 |
| 6 | **WS0-T1 zero blank rows — every row resolved or carrying pending-outreach status with dates** | [R] **zero blank cells**; **3** `(d) UNKNOWN` occurrences; all **3** `Outreach date` cells now state something true and dated (verified 2026-07-30 after E20); stated pending count **"two as of 2026-07-30"** | ✅ MET — ⚠ **see FLAG 2**: met only **as of 2026-07-30**. Until today two of the three rows satisfied this criterion on **false statements** (E20) |
| 7 | **WS5-T2a outreach sent** | [R] `docs/outreach/annotator-recruitment.md:3` — **SENT to Mayur021 2026-07-27**; the file itself asserts it satisfies this criterion | ✅ MET |
| 8 | **WS0-T3 reduction complete for sources already confirmed non-redistributable (AIAAIC now; others as their WS0-T1 rows resolve)** | [R] AIAAIC: **1,517** rows carry `content_license`, all `CC-BY-SA-4.0`. [R] **OECD: NOT reduced** — `ingest_oecd_aim.py:199-210` still builds `description` from `body["summary"]`, and **3,667** of those descriptions ship verbatim in `data/incidents.json` | 🔴 **NOT MET as written — see BLOCKER A (E21).** OECD's WS0-T1 row **is** resolved, so the criterion's own "others as their rows resolve" clause reaches it |
| 9 | **WS0-T4 done** *(D20(b)(i); split by D21 into clause 1 = code property, clause 2 = outreach)* | **Clause 1** [R]: `ingest/common.py` present, `scripts/ingest_utils.py` **gone**, `docs/INGESTION_CONDUCT.md` + `tests/test_network_chokepoint.py` present, plan invariant 5 reads the **D22-amended HTTP(S)** text with Active-from "WS0-T4 done"; enforcer green. Merged `8b1e8888`. [A] gated PASS after 1 bounce. **Clause 2** [R]: 3 of 4 recipients sent and dated (AIAAIC 07-27 + correction 07-29, AIID 07-27, MIT AIRI 07-27 + 07-29); **OECD drafted 2026-07-30, NOT sent** | **Clause 1 ✅ MET** (invariant 5 ACTIVE). **Clause 2 ⚠ OPEN** — see FLAG 3 |
| 10 | **WS4-T9 done — met only when every dead source has EITHER a remediation implemented OR a hold carrying a DECISION DATE** *(D20(b)(ii))* | [R] one dead source (`airi_navigator`, `status: stale`, `last_success: 2026-05-31`, `stale_since: 2026-06-07`); its hold is machine-readable in the shipped registry: **`{"decision":"D8","until":"2026-08-28"}`** (`data/source_freshness.json:26`), corroborated at `airi-draft4-export-request.md:5`. Freshness marker on **1,380** rows | ✅ MET — ⚠ **see FLAG 5**: this criterion was **unsatisfiable for 12 days** and D20(b)(ii) exists because of it |

## 🔴 BLOCKERS — criteria not met, or not honestly reportable, pending a user ruling

**BLOCKER A — criterion 8, WS0-T3/OECD (E21).** `data/incidents.json` ships
**3,667** entries whose `description` is OECD-derived narrative verbatim, while
`docs/SOURCE_LICENSES.md` §1.5 marks that text `Redistribute-verbatim: NO` and
its Action cell has called for reducing it to structural facts **since
2026-07-15**. The reduction was never built. At **2.6× the 1,422-row AIAAIC
reduction** this phase spent weeks on.

**It may nonetheless be a non-problem, and that is the ruling needed.** The gate
established by measurement that the `evidences` fallback fired for **0 of 4,160**
rows and that `summary` is a **composed OECD editorial abstract**, appearing
nowhere in the article-evidence text. Against the board's record that **OECD
written content published as of 2024-07-01 is CC BY 4.0**, §1.5's `NO` may
describe a code path that produced nothing. **Needed: commission the §1.5
re-analysis. If the compliance rationale is void, criterion 8 is MET and the
reduction becomes optional scope hygiene. If it stands, criterion 8 is NOT MET
and Phase 1 does not exit.** Do not rebuild 3,667 descriptions before this is
settled.

**BLOCKER B — the release itself (D19).** `CHANGELOG.md`'s newest entry is
`[2.8.0]` at 12,986 entries; the corpus is at **13,119** across **6
corpus-changing commits** since the tag, and no `v2.9.0` tag exists. The release
notes are drafted below, but the version bump, tag and CHANGELOG entry are
unwritten. This blocks the *release*, not the phase.

## ⚠ FLAGS — criteria satisfied, but a reader should know how

The user's stated reading standard: *"flag any criterion satisfied on a
TECHNICALITY, in the way the WS4-T9/undated-hold pattern was."* Five found. The
first is a defect in the board itself; the rest are honest-but-narrow passes.

**FLAG 1 — criterion 5's board row contradicts the decision that satisfies it.**
The row reads `done (draft; E4 decision now with user)`, but **D13 decided E4**
(Option B, padding-agnostic parsing commitment). The criterion is met; the row's
parenthetical is stale and would tell a reader the opposite. **Owner: foreman —
correct the row.** Flagged rather than silently fixed because a checklist that
edits its own evidence is worth less than one that reports it.

**FLAG 2 — criterion 6 was satisfied on false statements until today.** Two of
the three pending rows carried `"2026-07-15 (drafted; not yet sent)"` for drafts
that **had never existed on any branch** (E20 — `git log --all
--diff-filter=A -- 'docs/outreach/*'` returns the complete set; no OECD, no
CSET). This is the **sixth and seventh** instance of the E18 pattern. The
criterion is genuinely met **as of 2026-07-30**; it was not met on any earlier
date the board would have claimed it. **This is the closest analogue to the
undated-hold pattern the user named** — a criterion whose wording ("with dates")
was satisfiable by a date attached to nothing.

**FLAG 3 — criterion 9's clause 2 is 3-of-4, not 4-of-4.** OECD is **drafted
2026-07-30, gated PASS, not sent** — proposed recipient `ai@oecd.org`,
**UNCONFIRMED as to desk**. Per D21 this does **not** gate invariant 5, which is
a property of the code and is active. But the criterion as originally written
said "outreach emails drafted, **sent**, and logged with dates", and that is not
true of OECD. **Reported as open, not rounded up.** Closes when the user sends
and logs the date.

**FLAG 4 — criterion 2's live-claim check is a grep, and one banned phrasing
survives.** `CHANGELOG.md:137` still says **"Static TAXII 2.1 endpoint"**, the
banned phrasing while WS6-T4 is open; every other current surface says
"TAXII-compatible discovery document" / "read-only static mirror". Pre-existing
(logged at `a67acebc`), on a permanently-live public surface. The criterion
passes because the overclaim rule was enforced against the *live* surfaces;
`CHANGELOG` is history that is also published. **Owner: WS6.**

**FLAG 5 — criterion 10 is met by a date that did not exist for 12 days.**
D8 was decided 2026-07-17 and keyed its clock to sending AIRI Draft 4; **that
draft existed in no commit on any branch** (E18), so the clock could not start
and the hold was open-ended by construction until the redraft was written, gated
and sent 2026-07-29. D20(b)(ii) sharpened this criterion precisely so that state
reads as *unsatisfied*. It is now met — `{"decision":"D8","until":"2026-08-28"}`
— but the criterion's history is the reason its wording changed.

## Open items that do NOT block Phase-1 exit — recorded so the packet is complete

- **E22** — a 302 redirect defeats invariant 5's robots/pacing guarantees: the
  OECD sitemap redirects to `incidents-server.oecdai.org` and
  `ingest/common.py:364`/`:280` key off the pre-redirect host, with **zero**
  post-response `resp.url` re-checks. **Bounded: 1 fetch per weekly run** (of 8
  targets probed, the only redirect; individual incident pages do not redirect —
  7 probes, all 200/0 redirects). Owner WS4.
- **E23** — **all 1,517 `content_license` markers are AIAAIC; 0 of 1,464
  AIID-tagged rows carry one**, while `NOTICE-DATA`/`.reuse/dep5` describe the
  two exposures as "same posture". AIID's ShareAlike grant is the **confirmed**
  one. **This bears on the release notes below** — see the disclosure's scope
  caveat. Owner WS0 → WS3/WS4.
- **WS4 cache-write class audit** (opened `ce223784`) — the *trust-cache →
  swallow-`PermissionError` → write-unconditionally* signature, now at four
  instances, three found by adjacency rather than search. **B1** (a 9,264-entry
  file reduced to 1 without the guard firing) folds in.
- **A2** — OECD's 3,000 pages × 1.0 s serialise to a **~50-minute floor inside
  `auto-refresh.yml`'s 60-minute timeout**, cold cache every run. **P1 before the
  next weekly refresh**, and it now sits on active `main`.
- **D8 pass advisories A1/A2/A3**; docs-warden F5 (`exploited_in_wild`,
  `kev_date_added` undocumented), F8; **F3 = D20(a)**, already ruled, needs
  actioning on `.zenodo.json` and should ride v2.9.0.

## D19 packet — v2.9.0 release notes, DRAFT for user approval

**Not committed as a release. Draft only.** Version ruled by D19: **v2.9.0** —
minor, not patch, because it ships the licensing rebuild, the markers and the
notice surfaces (additive schema and data changes, no API break).

> ### v2.9.0 — Honest provenance
>
> This release completes the "Honest" phase of the improvement plan. It changes
> how the dataset describes its own provenance, licensing and freshness. No
> consumer-facing field was removed; the schema additions are additive.
>
> **Corpus:** 12,986 → **13,119** entries (+133 net; landmark 1,905). No entry
> was deleted — status and tombstones only (invariant 3).
>
> **Licensing and provenance**
> - **Row-level content-licence marking.** 1,517 AIAAIC-derived rows carry
>   `content_license: CC-BY-SA-4.0`, machine-readable, with the same value
>   mirrored into STIX as `x_content_license`. Rationale in `docs/audits/` (E13,
>   D11).
> - **AIAAIC facts-reduction.** AIAAIC-derived rows carry facts and a source
>   pointer rather than upstream narrative prose (D2/D9/D10).
> - **AIID ingestion moved to the sanctioned snapshot channel.** The former
>   high-volume scraper is disabled; AIID data now comes from AIID's own weekly
>   snapshot, with a committed pinned provenance record. AIID's excluded
>   `reports.text` is never opened.
> - **Source-licence register** (`docs/SOURCE_LICENSES.md`) now resolves every
>   row: **two** remain pending-with-dates (MIT AIRI Navigator, OECD AIM), down
>   from three.
>
> **Freshness honesty**
> - **`source_freshness`** ships on 1,380 rows derived from a source whose feed
>   went stale on 2026-05-31, carrying `status`, `as_of` and `sources`. `active`
>   means *emitted, not live*. Defined in `docs/DATA_DICTIONARY.md` and on the
>   HF card.
> - A published **source-freshness registry** (`data/source_freshness.json`)
>   declares which copy of the health counter it was reconciled against, and
>   `validate.py` holds it to that claim — reporting **VERIFIED**, **NOT
>   COMPARABLE HERE** or **FAILED** distinguishably. Passing means the registry
>   matches the copy it read; it is **not** a claim that the registry is current.
>
> **Ingestion conduct**
> - **A single network chokepoint.** All HTTP(S) fetching routes through
>   `ingest/common.py`, which enforces an identifying User-Agent, a fail-closed
>   robots.txt check and per-host rate limiting, with one enumerated, evidenced
>   host exception. Non-HTTP egress is **registered**, not exempted.
>   `docs/INGESTION_CONDUCT.md` states what the code enforces.
>
> ### ⚠ Supersession notice — read this if you consume v2.7.0 or v2.8.0
>
> **v2.7.0 and v2.8.0 both shipped mid-phase, before this licensing work
> landed.** If you are consuming **AIAAIC-derived rows** from either release you
> are on **pre-marker, pre-reduction data**: those rows carry no
> `content_license` marker and may carry upstream narrative prose that this
> release replaces with facts-plus-pointer. **v2.9.0 supersedes both for that
> purpose.** Re-pull rather than diffing.
>
> **Scope caveat, stated because it is currently open (E23):** this notice
> addresses AIAAIC-derived rows. **AIID-derived rows carry no `content_license`
> marker in this release either**, and whether they require one is an open
> question under review. If you redistribute AIID-derived rows, consult
> `docs/SOURCE_LICENSES.md` §1.2 and AIID's own terms rather than relying on the
> absence of a marker.

**⚠ The scope caveat above is why E23 wants an answer before the cut.** If AIID
rows need marking, the honest disclosure is not a caveat but a second marked
population — and the notice would be rewritten, not annotated.

## What this checklist does not claim

It does not claim Phase 1 is exitable today: **criterion 8 is not met as
written**, pending BLOCKER A. It does not claim the [A]-class rows were
independently re-verified by this document — their artifacts were checked, their
gate verdicts are testimony on the board. And it does not treat its own five
flags as disqualifying: four are honest-but-narrow passes, and the fifth (FLAG 1)
is a board defect this document reports rather than repairs.
