---
title: "INC-09239 — simple-git has blockUnsafeOperationsPlugin bypass via case-insensitive protocol.allow config key enables RCE"
layout: incident
permalink: /incident/INC-09239.html
incident_id: INC-09239
year: 2026
severity: Critical
description_preview: "### Summary The `blockUnsafeOperationsPlugin` in `simple-git` fails to block git protocol override arguments when the config key is passed in uppercase or mixed case. An attacker who controls argument"
tags_str: "command-injection, cve, ghsa, simple-git"
---

{% raw %}
<div class="incident-anchor" id="inc-09239" markdown="1">

### INC-09239  [↗](/incident/INC-09239.html)

**simple-git has blockUnsafeOperationsPlugin bypass via case-insensitive protocol.allow config key enables RCE**  
_2026-03 · vulnerability-disclosure · Severity: Critical_

CVEs: `CVE-2026-28292`
CWEs: `CWE-178`, `CWE-78`
CVSS: **9.8** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H`

### Summary The `blockUnsafeOperationsPlugin` in `simple-git` fails to block git protocol override arguments when the config key is passed in uppercase or mixed case. An attacker who controls arguments passed to git operations can enable the `ext::` protocol by passing `-c PROTOCOL.ALLOW=always`, which executes an arbitrary OS command on the host machine. --- ### Details The `preventProtocolOverride` function in `simple-git/src/lib/plugins/block-unsafe-operations-plugin.ts` (line 24) checks whether a `-c` argument configures `protocol.allow` using this regex: ```ts if (!/^\s*protocol(.[a-z]+)?.allow/.test(next)) { return; } ``` This regex is case-sensitive. Git treats config key names case-insensitively — it normalises them to lowercase internally. As a result, passing `PROTOCOL.ALLOW=always`, `Protocol.Allow=always`, or any mixed-case variant is not matched by the regex, the check returns without throwing, and git is spawned with the unsafe argument. **Verification that git normalises the key:** ```bash $ git -c PROTOCOL.ALLOW=always config --list | grep protocol protocol.allow=always ``` **The fix is a single character — add the `/i` flag:** ```ts // Before (vulnerable): if (!/^\s*protocol(.[a-z]+)?.allow/.test(next)) { // After (fixed): if (!/^\s*protocol(.[a-z]+)?.allow/i.test(next)) { ``` --- ## poc.js ```js /** * Proof of Concept — simple-git preventProtocolOverride Case-Sensitivity Bypass * * CVE-2022-25912 was fixed in simple-git@3.15.0 by adding a

**Affected:** npm/simple-git  
**Attack vector:** `command-injection`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-28292) _(advisory)_
- [GHSA-r275-fr43-pm7q](https://github.com/advisories/GHSA-r275-fr43-pm7q) _(advisory)_
- [https://github.com/steveukx/git-js/commit/f7042088aa2dac59e3c49a84d7a2f4b26048a257](https://github.com/steveukx/git-js/commit/f7042088aa2dac59e3c49a84d7a2f4b26048a257) _(reference)_
- [https://www.codeant.ai/security-research/security-research-simple-git-remote-code-execution-cve-2026-28292](https://www.codeant.ai/security-research/security-research-simple-git-remote-code-execution-cve-2026-28292) _(reference)_
- [https://github.com/steveukx/git-js/security/advisories/GHSA-r275-fr43-pm7q](https://github.com/steveukx/git-js/security/advisories/GHSA-r275-fr43-pm7q) _(reference)_
- [https://www.codeant.ai/security-research/simple-git-remote-code-execution-cve-2026-28292](https://www.codeant.ai/security-research/simple-git-remote-code-execution-cve-2026-28292) _(reference)_

**Tags:** `command-injection`, `cve`, `ghsa`, `simple-git`

</div>

{% endraw %}
