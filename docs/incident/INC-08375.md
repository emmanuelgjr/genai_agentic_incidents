---
title: "INC-08375 — NodeVM network builtin exclusions bypass via internal _http_client and _http_server"
layout: incident
permalink: /incident/INC-08375.html
incident_id: INC-08375
year: 2026
severity: High
description_preview: "## Summary `NodeVM` supports excluding public network builtins from the wildcard builtin option. With this configuration direct access to `http`, `https`, `http2`, `net`, `dgram`, `tls`, `dns`, and `d"
tags_str: "cve, ghsa, ssrf, vm2"
---

{% raw %}
<div class="incident-anchor" id="inc-08375" markdown="1">

### INC-08375  [↗](/incident/INC-08375.html)

**NodeVM network builtin exclusions bypass via internal _http_client and _http_server**  
_2026-05 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-47139`
CWEs: `CWE-693`
CVSS: **8.6** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:N/A:N`

## Summary `NodeVM` supports excluding public network builtins from the wildcard builtin option. With this configuration direct access to `http`, `https`, `http2`, `net`, `dgram`, `tls`, `dns`, and `dns/promises` is blocked. However, Node.js also exposes underscored internal HTTP builtins such as `_http_client` and `_http_server`. These are not blocked when the public modules are excluded. Sandboxed code can use these internal builtins to make outbound HTTP requests and open listening HTTP sockets even though the public network modules are denied. **Note**: This is not host RCE. It is a network capability bypass that can lead to SSRF-style access to internal services. ## Details The wildcard builtin expansion is based on Node.js builtin module names: ```js const BUILTIN_MODULES = (nmod.builtinModules || Object.getOwnPropertyNames(process.binding('natives'))) .filter(s=>!s.startsWith('internal/') && !DANGEROUS_BUILTINS.has(s)); ``` Public modules can be excluded with `-name`: ```js if (builtins.indexOf(`-${name}`) === -1) { addDefaultBuiltin(res, name, hostRequire); } ``` But excluding `http` and `net` does not exclude internal siblings such as: ```text _http_client _http_server _tls_wrap ``` These internal modules expose network primitives. Confirmed examples: 1. `require('_http_client').ClientRequest(...)` performs an outbound HTTP request to a host-local service while `http` and `net` are blocked. 2. `require('_http_server').Server(...).listen(...)` opens

**Affected:** npm/vm2  
**Attack vector:** `ssrf`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0049`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-47139) _(advisory)_
- [GHSA-r9pm-gxmw-wv6p](https://github.com/advisories/GHSA-r9pm-gxmw-wv6p) _(advisory)_
- [https://github.com/patriksimek/vm2/security/advisories/GHSA-r9pm-gxmw-wv6p](https://github.com/patriksimek/vm2/security/advisories/GHSA-r9pm-gxmw-wv6p) _(reference)_
- [https://github.com/patriksimek/vm2/commit/436053e30eecbabd487e2fd2959c137ac34e2bb1](https://github.com/patriksimek/vm2/commit/436053e30eecbabd487e2fd2959c137ac34e2bb1) _(reference)_
- [https://github.com/patriksimek/vm2/releases/tag/v3.11.4](https://github.com/patriksimek/vm2/releases/tag/v3.11.4) _(reference)_

**Tags:** `cve`, `ghsa`, `ssrf`, `vm2`

</div>

{% endraw %}
