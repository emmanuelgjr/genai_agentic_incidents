---
title: "INC-08220 — nebula-mesh's web UI lacks CSRF tokens on /ui/* mutating endpoints"
layout: incident
permalink: /incident/INC-08220.html
incident_id: INC-08220
year: 2026
severity: High
description_preview: "Every `/ui/*` POST / PUT / PATCH / DELETE route processes the request as soon as the session cookie validates. `SameSite=Lax` on the session cookie prevents most cross-site form submits but does not p"
tags_str: "cve, ghsa, github.com-juev-nebula-mesh, xss"
---

<div class="incident-anchor" id="inc-08220" markdown="1">

### INC-08220  [↗](/incident/INC-08220.html)

**nebula-mesh's web UI lacks CSRF tokens on /ui/* mutating endpoints**  
_2026-06 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-47725`
CWEs: `CWE-269`, `CWE-352`

Every `/ui/*` POST / PUT / PATCH / DELETE route processes the request as soon as the session cookie validates. `SameSite=Lax` on the session cookie prevents most cross-site form submits but does not protect: - top-level form-submit navigations from third-party pages (some browsers still send Lax cookies on top-level POSTs) - same-registrable-domain attackers (sibling-subdomain XSS, subdomain takeover) - the `GET /ui/logout` route, which a third-party `<img src=".../ui/logout">` can force-trigger The admin UI signs CA certificates, mints API keys, rotates / retires / deletes CAs, disables operators, and changes server settings. CSRF here is a real privilege escalation, not just annoyance. ## Affected All released versions up to v0.3.2. ## Suggested fix Double-submit cookie: a 32-byte `crypto/rand` token in a non-HttpOnly `_csrf` cookie, echoed in either `X-CSRF-Token` (htmx) or a `_csrf` form field (HTML forms). Compared in constant time. Rotated on every privilege transition (`Login`, OIDC `StartAuthenticatedSession`, `CompleteTwoFactor`, `Logout`) so pre-auth fixation cannot survive promotion. Rejections audit-logged as `ui.csrf.rejected` with reason; response body stays opaque. `/ui/logout` becomes POST so it is no longer reachable via `<img>` tags. Fix coordinates with the Secure-cookie advisory disclosed concurrently — the `_csrf` cookie inherits the same `Secure`-attribute derivation. ## Reproducer With an authenticated operator session in browser tab A, open the

**Affected:** go/github.com/juev/nebula-mesh  
**Attack vector:** `xss`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-47725) _(advisory)_
- [GHSA-273q-qgh5-wrj6](https://github.com/advisories/GHSA-273q-qgh5-wrj6) _(advisory)_
- [https://github.com/juev/nebula-mesh/security/advisories/GHSA-273q-qgh5-wrj6](https://github.com/juev/nebula-mesh/security/advisories/GHSA-273q-qgh5-wrj6) _(reference)_

**Tags:** `cve`, `ghsa`, `github.com-juev-nebula-mesh`, `xss`

</div>

