---
title: "INC-09209 — Dagu: Path Traversal via `dagRunId` in Inline DAG Execution"
layout: incident
permalink: /incident/INC-09209.html
incident_id: INC-09209
year: 2026
severity: Critical
description_preview: "## 1. Vulnerability Summary The `dagRunId` request field accepted by the inline DAG execution endpoints is passed directly into `filepath.Join` to construct a temporary directory path without any form"
tags_str: "cve, ghsa, github.com-dagu-org-dagu, path-traversal"
---

<div class="incident-anchor" id="inc-09209" markdown="1">

### INC-09209  [↗](/incident/INC-09209.html)

**Dagu: Path Traversal via `dagRunId` in Inline DAG Execution**  
_2026-03 · vulnerability-disclosure · Severity: Critical_

CVEs: `CVE-2026-31886`
CWEs: `CWE-22`
CVSS: **9.1** — `CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:L/I:L/A:H`

## 1. Vulnerability Summary The `dagRunId` request field accepted by the inline DAG execution endpoints is passed directly into `filepath.Join` to construct a temporary directory path without any format validation. Go's `filepath.Join` resolves `..` segments lexically, so a caller can supply a value such as `".."` to redirect the computed directory outside the intended `/tmp/<name>/<id>` path. A deferred cleanup function that calls `os.RemoveAll` on that directory then runs unconditionally when the HTTP handler returns, deleting whatever directory the traversal resolved to. With `dagRunId` set to `".."`, the resolved directory is the system temporary directory (`/tmp` on Linux). On non-root deployments, `os.RemoveAll("/tmp")` removes all files in `/tmp` owned by the dagu process user, disrupting every concurrent dagu run that has live temp files. On root or Docker deployments, the call removes the entire contents of `/tmp`, causing a system-wide denial of service. ## 2. This Is Not a Duplicate of Existing Advisories Two security advisories are already published for dagu. This vulnerability is distinct from both. **GHSA-6qr9-g2xw-cw92** fixed the fact that the default authentication mode was `none`, allowing unauthenticated access to the inline execution endpoint. That advisory covers authentication bypass. The `dagRunId` path traversal described here is a separate input-validation flaw in `loadInlineDAG()` that exists regardless of whether authentication is required and

**Affected:** go/github.com/dagu-org/dagu  
**Attack vector:** `path-traversal`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-31886) _(advisory)_
- [GHSA-m4q3-457p-hh2x](https://github.com/advisories/GHSA-m4q3-457p-hh2x) _(advisory)_
- [https://github.com/dagu-org/dagu/security/advisories/GHSA-m4q3-457p-hh2x](https://github.com/dagu-org/dagu/security/advisories/GHSA-m4q3-457p-hh2x) _(reference)_
- [https://github.com/dagu-org/dagu/commit/12c2e5395bd9331d49ca103593edfd0db39c4f38](https://github.com/dagu-org/dagu/commit/12c2e5395bd9331d49ca103593edfd0db39c4f38) _(reference)_
- [https://pkg.go.dev/vuln/GO-2026-4693](https://pkg.go.dev/vuln/GO-2026-4693) _(reference)_

**Tags:** `cve`, `ghsa`, `github.com-dagu-org-dagu`, `path-traversal`

</div>

