---
title: "INC-09011 — xmldom: XML injection via unsafe CDATA serialization allows attacker-controlled markup insertion"
layout: incident
permalink: /incident/INC-09011.html
incident_id: INC-09011
year: 2026
severity: High
description_preview: "## Summary `@xmldom/xmldom` allows attacker-controlled strings containing the CDATA terminator `]]>` to be inserted into a `CDATASection` node. During serialization, `XMLSerializer` emitted the CDATA"
tags_str: "cve, ghsa, xmldom, xmldom-xmldom"
---

<div class="incident-anchor" id="inc-09011" markdown="1">

### INC-09011  [↗](/incident/INC-09011.html)

**xmldom: XML injection via unsafe CDATA serialization allows attacker-controlled markup insertion**  
_2026-04 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-34601`
CWEs: `CWE-91`
CVSS: **7.5** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:H/A:N`

## Summary `@xmldom/xmldom` allows attacker-controlled strings containing the CDATA terminator `]]>` to be inserted into a `CDATASection` node. During serialization, `XMLSerializer` emitted the CDATA content verbatim without rejecting or safely splitting the terminator. As a result, data intended to remain text-only became **active XML markup** in the serialized output, enabling XML structure injection and downstream business-logic manipulation. The sequence `]]>` is not allowed inside CDATA content and must be rejected or safely handled during serialization. ([MDN Web Docs](https://developer.mozilla.org/)) ### Attack surface `Document.createCDATASection(data)` is the most direct entry point, but it is not the only one. The WHATWG DOM spec intentionally does not validate `]]>` in mutation methods — only `createCDATASection` carries that guard. The following paths therefore also allow `]]>` to enter a CDATASection node and reach the serializer: - `CharacterData.appendData()` - `CharacterData.replaceData()` - `CharacterData.insertData()` - Direct assignment to `.data` - Direct assignment to `.textContent` (Note: assigning to `.nodeValue` does **not** update `.data` in this implementation — the serializer reads `.data` directly — so `.nodeValue` is not an exploitable path.) ### Parse path Parsing XML that contains a CDATA section is **not** affected. The SAX parser's non-greedy `CDSect` regex stops at the first `]]>`, so parsed CDATA data never contains the terminator.

**Affected:** npm/@xmldom/xmldom, npm/@xmldom/xmldom, npm/xmldom  
**Attack vector:** `other`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI04`  
**NIST AI RMF:** `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-34601) _(advisory)_
- [GHSA-wh4c-j3r5-mjhp](https://github.com/advisories/GHSA-wh4c-j3r5-mjhp) _(advisory)_
- [https://github.com/xmldom/xmldom/security/advisories/GHSA-wh4c-j3r5-mjhp](https://github.com/xmldom/xmldom/security/advisories/GHSA-wh4c-j3r5-mjhp) _(reference)_
- [https://github.com/xmldom/xmldom/commit/2b852e836ab86dbbd6cbaf0537f584dd0b5ac184](https://github.com/xmldom/xmldom/commit/2b852e836ab86dbbd6cbaf0537f584dd0b5ac184) _(reference)_
- [https://github.com/xmldom/xmldom/releases/tag/0.8.12](https://github.com/xmldom/xmldom/releases/tag/0.8.12) _(reference)_
- [https://github.com/xmldom/xmldom/releases/tag/0.9.9](https://github.com/xmldom/xmldom/releases/tag/0.9.9) _(reference)_

**Tags:** `cve`, `ghsa`, `xmldom`, `xmldom-xmldom`

</div>

