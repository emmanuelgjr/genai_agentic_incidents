---
title: "INC-09045 — @tinacms/graphql has Path Traversal that leads to overwrite of arbitrary files"
layout: incident
permalink: /incident/INC-09045.html
incident_id: INC-09045
year: 2026
severity: High
description_preview: "### Summary A Path Traversal vulnerability in `@tinacms/graphql` allows unauthenticated users to write and overwrite arbitrary files within the project root. This is achieved by manipulating the `rela"
tags_str: "cve, ghsa, path-traversal, tinacms-graphql"
---

<div class="incident-anchor" id="inc-09045" markdown="1">

### INC-09045  [↗](/incident/INC-09045.html)

**@tinacms/graphql has Path Traversal that leads to overwrite of arbitrary files**  
_2026-03 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-33949`
CWEs: `CWE-22`, `CWE-73`
CVSS: **8.1** — `CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:N/I:H/A:H`

### Summary A Path Traversal vulnerability in `@tinacms/graphql` allows unauthenticated users to write and overwrite arbitrary files within the project root. This is achieved by manipulating the `relativePath` parameter in GraphQL mutations. The impact includes the ability to replace critical server configuration files and potentially execute arbitrary commands by sabotaging build scripts. ### Details The vulnerability exists in the path validation logic within `@tinacms/graphql`. Specifically, the regex-based validation in `getValidatedPath` fails to recognize backslashes (`\`) as directory separators on non-Windows platforms (Mac/Linux). An attacker can provide a path like `x\..\..\..\package.json`, which bypasses the validation check but is subsequently treated as a traversal path during file I/O operations by the underlying `fs` modules and path normalization utilities. Incriminated code areas: - [packages/@tinacms/graphql/src/database/bridge/filesystem.ts](tinacms/packages/@tinacms/graphql/src/database/bridge/filesystem.ts): [assertWithinBase](tinacms/graphql/src/database/bridge/filesystem.ts#7-35) function. - [packages/@tinacms/graphql/src/resolver/index.ts](tinacms/packages/@tinacms/graphql/src/resolver/index.ts): `getValidatedPath` function. ### PoC 1. Start the TinaCMS development server. 2. Send a malicious GraphQL mutation to overwrite a project file (e.g., [package.json](tinacms/examples/tina-self-hosted-demo/package.json)): ```bash curl -X POST http://localho

**Affected:** npm/@tinacms/graphql  
**Attack vector:** `path-traversal`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-33949) _(advisory)_
- [GHSA-v9p7-gf3q-h779](https://github.com/advisories/GHSA-v9p7-gf3q-h779) _(advisory)_
- [https://github.com/tinacms/tinacms/security/advisories/GHSA-v9p7-gf3q-h779](https://github.com/tinacms/tinacms/security/advisories/GHSA-v9p7-gf3q-h779) _(reference)_

**Tags:** `cve`, `ghsa`, `path-traversal`, `tinacms-graphql`

</div>

