---
title: "INC-08229 — NASA AMMOS Instrument Toolkit: Path traversal resulting in arbitrary file append (can be triggered over the network by unauthenticated attacker)"
layout: incident
permalink: /incident/INC-08229.html
incident_id: INC-08229
year: 2026
severity: Critical
description_preview: "## 1. Summary The Binary Stream Capture (BSC) component exposes an unauthenticated HTTP API for dynamically creating packet capture “handlers.” Because the code blindly trusts path‑related form fields"
tags_str: "ait-core, cve, ghsa, path-traversal"
---

{% raw %}
<div class="incident-anchor" id="inc-08229" markdown="1">

### INC-08229  [↗](/incident/INC-08229.html)

**NASA AMMOS Instrument Toolkit: Path traversal resulting in arbitrary file append (can be triggered over the network by unauthenticated attacker)**  
_2026-06 · vulnerability-disclosure · Severity: Critical_

CVEs: `CVE-2026-47731`
CWEs: `CWE-22`
CVSS: **9.1** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:H/A:H`

## 1. Summary The Binary Stream Capture (BSC) component exposes an unauthenticated HTTP API for dynamically creating packet capture “handlers.” Because the code blindly trusts path‑related form fields, a remote client can: - **Bypass the configured log root** and direct BSC to log to **arbitrary filesystem paths** (path traversal / directory escape), and - **Append attacker‑controlled data** to those files, using the privileges of the`ait-bsc` process. There are two ways for a remote attacker to trigger this: 1. If the attacker has access to the network where `ait-bsc` is deployed (a reason for that could be that the ports are publicly accessible), the payloads can be directly sent to the server to trigger the arbitrary file append. This type of attack is demonstrated in `python_poc.py`. 2. Even if the attacker does not have direct access to the network because the software is running in a local network, it is possible to exploit this if a bad actor in that network opens an attacker-controlled website (which might be a website created by an attacker, or a third-party website compromised by the attacker). The browser javascript can automatically send the requests necessary to exploit this into the local network. This is even possible if the server is only accessible on `localhost`. This type of attack is demonstrated by `attacker_tcp.py` and `test1.html` (first launch the attacker TCP server, then start a webserver to host `test1.html`, for example using `python3 -m http.se

**Affected:** pip/ait-core, pip/ait-core  
**Attack vector:** `path-traversal`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-47731) _(advisory)_
- [GHSA-p462-prxw-mjx4](https://github.com/advisories/GHSA-p462-prxw-mjx4) _(advisory)_
- [https://github.com/NASA-AMMOS/AIT-Core/security/advisories/GHSA-p462-prxw-mjx4](https://github.com/NASA-AMMOS/AIT-Core/security/advisories/GHSA-p462-prxw-mjx4) _(reference)_
- [https://github.com/NASA-AMMOS/AIT-Core/releases/tag/2.6.1](https://github.com/NASA-AMMOS/AIT-Core/releases/tag/2.6.1) _(reference)_
- [https://github.com/NASA-AMMOS/AIT-Core/releases/tag/3.1.1](https://github.com/NASA-AMMOS/AIT-Core/releases/tag/3.1.1) _(reference)_

**Tags:** `ait-core`, `cve`, `ghsa`, `path-traversal`

</div>

{% endraw %}
