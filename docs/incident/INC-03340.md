---
title: "INC-03340 — vLLM — Dos (CVE-2025-29770)"
layout: incident
permalink: /incident/INC-03340.html
incident_id: INC-03340
year: 2025
severity: Medium
description_preview: "vLLM is a high-throughput and memory-efficient inference and serving engine for LLMs. The outlines library is one of the backends used by vLLM to support structured output (a.k.a. guided decoding). Ou"
tags_str: "cve, nvd, vllm"
---

<div class="incident-anchor" id="inc-03340" markdown="1">

### INC-03340  [↗](/incident/INC-03340.html)

**vLLM — Dos (CVE-2025-29770)**  
_2025-03 · vulnerability-disclosure · Severity: Medium_

CVEs: `CVE-2025-29770`
CVSS: **6.5**

vLLM is a high-throughput and memory-efficient inference and serving engine for LLMs. The outlines library is one of the backends used by vLLM to support structured output (a.k.a. guided decoding). Outlines provides an optional cache for its compiled grammars on the local filesystem. This cache has been on by default in vLLM. Outlines is also available by default through the OpenAI compatible API server. The affected code in vLLM is vllm/model_executor/guided_decoding/outlines_logits_processors.py, which unconditionally uses the cache from outlines. A malicious user can send a stream of very short decoding requests with unique schemas, resulting in an addition to the cache for each request. This can result in a Denial of Service if the filesystem runs out of space. Note that even if vLLM was configured to use a different backend by default, it is still possible to choose outlines on a per-request basis using the guided_decoding_backend key of the extra_body field of the request. This issue applies only to the V0 engine and is fixed in 0.8.0.

**Affected:** vllm/vllm  
**Attack vector:** `dos`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI04`  
**NIST AI RMF:** `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2025-29770) _(advisory)_
- [security-advisories@github.com](https://github.com/vllm-project/vllm/blob/53be4a863486d02bd96a59c674bbec23eec508f6/vllm/model_executor/guided_decoding/outlines_logits_processors.py) _(reference)_
- [security-advisories@github.com](https://github.com/vllm-project/vllm/pull/14837) _(patch)_
- [security-advisories@github.com](https://github.com/vllm-project/vllm/security/advisories/GHSA-mgrm-fgjv-mhv8) _(vendor)_

**Tags:** `cve`, `nvd`, `vllm`

</div>

