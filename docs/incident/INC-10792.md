---
title: "INC-10792 — Butterfly has path/URL confusion in resource handling leading to multiple weaknesses"
layout: incident
permalink: /incident/INC-10792.html
incident_id: INC-10792
year: 2024
severity: Critical
description_preview: "### Summary The Butterfly framework uses the `java.net.URL` class to refer to (what are expected to be) local resource files, like images or templates. This works: \"opening a connection\" to these URLs"
tags_str: "cve, ghsa, org.openrefine.dependencies-butterfly, ssrf"
---

{% raw %}
<div class="incident-anchor" id="inc-10792" markdown="1">

### INC-10792  [↗](/incident/INC-10792.html)

**Butterfly has path/URL confusion in resource handling leading to multiple weaknesses**  
_2024-10 · vulnerability-disclosure · Severity: Critical_

CVEs: `CVE-2024-47883`
CWEs: `CWE-22`, `CWE-36`, `CWE-918`
CVSS: **9.1** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N`

### Summary The Butterfly framework uses the `java.net.URL` class to refer to (what are expected to be) local resource files, like images or templates. This works: "opening a connection" to these URLs opens the local file. However, if a `file:/` URL is directly given where a relative path (resource name) is expected, this is also accepted in some code paths; the app then fetches the file, from a remote machine if indicated, and uses it as if it was a trusted part of the app's codebase. This leads to multiple weaknesses and potential weaknesses: * An attacker that has network access to the application could use it to gain access to files, either on the the server's filesystem (path traversal) or shared by nearby machines (server-side request forgery with e.g. SMB). * An attacker that can lead or redirect a user to a crafted URL belonging to the app could cause arbitrary attacker-controlled JavaScript to be loaded in the victim's browser (cross-site scripting). * If an app is written in such a way that an attacker can influence the resource name used for a template, that attacker could cause the app to fetch and execute an attacker-controlled template (remote code execution). ### Details The `edu.mit.simile.butterfly.ButterflyModuleImpl.getResource` method converts a resource name into an URL, for instance: ``` images/logo-gem-126.svg file:/C:/Users/Wander/IdeaProjects/OpenRefine/main/webapp/modules/core/images/logo-gem-126.svg ``` If the resource name already starts wit

**Affected:** maven/org.openrefine.dependencies:butterfly  
**Attack vector:** `ssrf`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0049`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2024-47883) _(advisory)_
- [GHSA-3p8v-w8mr-m3x8](https://github.com/advisories/GHSA-3p8v-w8mr-m3x8) _(advisory)_
- [https://github.com/OpenRefine/simile-butterfly/security/advisories/GHSA-3p8v-w8mr-m3x8](https://github.com/OpenRefine/simile-butterfly/security/advisories/GHSA-3p8v-w8mr-m3x8) _(reference)_
- [https://github.com/OpenRefine/simile-butterfly/commit/537f64bfa72746f8b21d4bda461fad843435319c](https://github.com/OpenRefine/simile-butterfly/commit/537f64bfa72746f8b21d4bda461fad843435319c) _(reference)_

**Tags:** `cve`, `ghsa`, `org.openrefine.dependencies-butterfly`, `ssrf`

</div>

{% endraw %}
