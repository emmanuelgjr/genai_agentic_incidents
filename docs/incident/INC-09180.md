---
title: "INC-09180 — AVideo vulnerable to unauthenticated SSRF via HTTP redirect bypass in LiveLinks proxy"
layout: incident
permalink: /incident/INC-09180.html
incident_id: INC-09180
year: 2026
severity: High
description_preview: "## Summary The `plugin/LiveLinks/proxy.php` endpoint validates user-supplied URLs against internal/private networks using `isSSRFSafeURL()`, but only checks the initial URL. When the initial URL respo"
tags_str: "cve, ghsa, ssrf, wwbn-avideo"
---

<div class="incident-anchor" id="inc-09180" markdown="1">

### INC-09180  [↗](/incident/INC-09180.html)

**AVideo vulnerable to unauthenticated SSRF via HTTP redirect bypass in LiveLinks proxy**  
_2026-03 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-33039`
CWEs: `CWE-918`
CVSS: **8.6** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:N/A:N`

## Summary The `plugin/LiveLinks/proxy.php` endpoint validates user-supplied URLs against internal/private networks using `isSSRFSafeURL()`, but only checks the initial URL. When the initial URL responds with an HTTP redirect (`Location` header), the redirect target is fetched via `fakeBrowser()` without re-validation, allowing an attacker to reach internal services (cloud metadata, RFC1918 addresses) through an attacker-controlled redirect. ## Affected Component - `plugin/LiveLinks/proxy.php` — lines 38-42 (redirect handling without SSRF re-validation) - `objects/functionsBrowser.php` — `fakeBrowser()` (line 123, raw cURL fetch with no SSRF protections) ## Description ### Missing SSRF re-validation after HTTP redirect The `proxy.php` endpoint validates the user-supplied `livelink` parameter against internal networks on line 18, using the comprehensive `isSSRFSafeURL()` function (which blocks private IPs, loopback, link-local/metadata, cloud metadata hostnames, and resolves DNS to detect rebinding). However, after calling `get_headers()` on line 38 — which follows HTTP redirects — the code extracts the `Location` header and passes it directly to `fakeBrowser()` without re-applying the SSRF check: ```php // plugin/LiveLinks/proxy.php — lines 17-42 // SSRF Protection: Block requests to internal/private networks if (!isSSRFSafeURL($_GET['livelink'])) { // line 18: only checks initial URL _error_log("LiveLinks proxy: SSRF protection blocked URL: " . $

**Affected:** composer/wwbn/avideo  
**Attack vector:** `ssrf`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`, `AML.T0049`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-33039) _(advisory)_
- [GHSA-9x67-f2v7-63rw](https://github.com/advisories/GHSA-9x67-f2v7-63rw) _(advisory)_
- [https://github.com/WWBN/AVideo/security/advisories/GHSA-9x67-f2v7-63rw](https://github.com/WWBN/AVideo/security/advisories/GHSA-9x67-f2v7-63rw) _(reference)_
- [https://github.com/WWBN/AVideo/commit/0e56382921fc71e64829cd1ec35f04e338c70917](https://github.com/WWBN/AVideo/commit/0e56382921fc71e64829cd1ec35f04e338c70917) _(reference)_

**Tags:** `cve`, `ghsa`, `ssrf`, `wwbn-avideo`

</div>

