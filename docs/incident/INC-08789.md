---
title: "INC-08789 — Astro: XSS in define:vars via incomplete </script> tag sanitization"
layout: incident
permalink: /incident/INC-08789.html
incident_id: INC-08789
year: 2026
severity: Medium
description_preview: "## Summary The `defineScriptVars` function in Astro's server-side rendering pipeline uses a case-sensitive regex `/<\\/script>/g` to sanitize values injected into inline `<script>` tags via the `define"
tags_str: "astro, cve, ghsa"
---

<div class="incident-anchor" id="inc-08789" markdown="1">

### INC-08789  [↗](/incident/INC-08789.html)

**Astro: XSS in define:vars via incomplete </script> tag sanitization**  
_2026-04 · vulnerability-disclosure · Severity: Medium_

CVEs: `CVE-2026-41067`
CWEs: `CWE-79`
CVSS: **6.1** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:L/I:L/A:N`

## Summary The `defineScriptVars` function in Astro's server-side rendering pipeline uses a case-sensitive regex `/<\/script>/g` to sanitize values injected into inline `<script>` tags via the `define:vars` directive. HTML parsers close `<script>` elements case-insensitively and also accept whitespace or `/` before the closing `>`, allowing an attacker to bypass the sanitization with payloads like `</Script>`, `</script >`, or `</script/>` and inject arbitrary HTML/JavaScript. ## Details The vulnerable function is `defineScriptVars` at `packages/astro/src/runtime/server/render/util.ts:42-53`: ```typescript export function defineScriptVars(vars: Record<any, any>) { let output = ''; for (const [key, value] of Object.entries(vars)) { output += `const ${toIdent(key)} = ${JSON.stringify(value)?.replace( /<\/script>/g, // ← Case-sensitive, exact match only '\\x3C/script>', )};\n`; } return markHTMLString(output); } ``` This function is called from `renderElement` at `util.ts:172-174` when a `<script>` element has `define:vars`: ```typescript if (name === 'script') { delete props.hoist; children = defineScriptVars(defineVars) + '\n' + children; } ``` The regex `/<\/script>/g` fails to match three classes of closing script tags that HTML parsers accept per the [HTML specification §13.2.6.4](https://html.spec.whatwg.org/multipage/parsing.html#parsing-main-inbody): 1. **Case variations**: `</Script>`, `</SCRIPT>`, `</sCrIpT>` — HTML tag names are case-inse

**Affected:** npm/astro  
**Attack vector:** `xss`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-41067) _(advisory)_
- [GHSA-j687-52p2-xcff](https://github.com/advisories/GHSA-j687-52p2-xcff) _(advisory)_
- [https://github.com/withastro/astro/security/advisories/GHSA-j687-52p2-xcff](https://github.com/withastro/astro/security/advisories/GHSA-j687-52p2-xcff) _(reference)_
- [https://github.com/withastro/astro/releases/tag/astro@6.1.6](https://github.com/withastro/astro/releases/tag/astro@6.1.6) _(reference)_

**Tags:** `astro`, `cve`, `ghsa`

</div>

