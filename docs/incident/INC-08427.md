---
title: "INC-08427 — Twig: HTML-output filters in twig/* extras incorrectly declared `is_safe => ['all']`"
layout: incident
permalink: /incident/INC-08427.html
incident_id: INC-08427
year: 2026
severity: Low
description_preview: "### Description Several filters in the `twig/*` extras packages are registered with `is_safe => ['all']`, which tells Twig's autoescaper to treat their output as safe in every context (`html`, `js`, `"
tags_str: "cve, ghsa, twig-cssinliner-extra, twig-markdown-extra, xss"
---

{% raw %}
<div class="incident-anchor" id="inc-08427" markdown="1">

### INC-08427  [↗](/incident/INC-08427.html)

**Twig: HTML-output filters in twig/* extras incorrectly declared `is_safe => ['all']`**  
_2026-05 · vulnerability-disclosure · Severity: Low_

CVEs: `CVE-2026-46637`
CWEs: `CWE-116`

### Description Several filters in the `twig/*` extras packages are registered with `is_safe => ['all']`, which tells Twig's autoescaper to treat their output as safe in every context (`html`, `js`, `css`, `url`, ...). The output of these filters is plain text or HTML markup, neither of which is safe in every escaping context. Affected filters: - `html_to_markdown` (`twig/markdown-extra`) emits plain Markdown text. `league/html-to-markdown` decodes HTML entities when producing code spans and fenced blocks, so an attacker-controlled `<code>&lt;img src=x onerror=alert(1)&gt;</code>` becomes `` `<img src=x onerror=alert(1)>` ``, which renders live when interpolated into an HTML page. - `markdown_to_html` (`twig/markdown-extra`) emits HTML. Safe in an HTML context but not in JS, CSS or URL contexts (e.g. when interpolated into an inline `<script>` block). - `inline_css` (`twig/cssinliner-extra`) emits HTML with inlined styles. Same constraint as `markdown_to_html`. In all three cases, `is_safe => ['all']` causes the autoescaper to emit the output verbatim in any context, even when the developer never wrote `|raw`. In a context such as a JS string or a URL parameter, this produces unescaped HTML and is exploitable as XSS. ### Resolution - `html_to_markdown` no longer claims to be safe in any escaping context; its plain-text output is now autoescaped for the surrounding context. - `markdown_to_html` and `inline_css` are now declared `is_safe => ['html']`, asserting only what

**Affected:** composer/twig/cssinliner-extra, composer/twig/markdown-extra  
**Attack vector:** `xss`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-46637) _(advisory)_
- [GHSA-jv8m-2544-3pg3](https://github.com/advisories/GHSA-jv8m-2544-3pg3) _(advisory)_
- [https://github.com/twigphp/Twig/security/advisories/GHSA-jv8m-2544-3pg3](https://github.com/twigphp/Twig/security/advisories/GHSA-jv8m-2544-3pg3) _(reference)_
- [https://github.com/FriendsOfPHP/security-advisories/blob/master/twig/cssinliner-extra/CVE-2026-46637.yaml](https://github.com/FriendsOfPHP/security-advisories/blob/master/twig/cssinliner-extra/CVE-2026-46637.yaml) _(reference)_
- [https://github.com/FriendsOfPHP/security-advisories/blob/master/twig/markdown-extra/CVE-2026-46637.yaml](https://github.com/FriendsOfPHP/security-advisories/blob/master/twig/markdown-extra/CVE-2026-46637.yaml) _(reference)_
- [https://symfony.com/cve-2026-46637](https://symfony.com/cve-2026-46637) _(reference)_

**Tags:** `cve`, `ghsa`, `twig-cssinliner-extra`, `twig-markdown-extra`, `xss`

</div>

{% endraw %}
