---
title: "INC-09363 — zae-limiter: DynamoDB hot partition throttling enables per-entity Denial of Service"
layout: incident
permalink: /incident/INC-09363.html
incident_id: INC-09363
year: 2026
severity: Medium
description_preview: "## Summary All rate limit buckets for a single entity share the same DynamoDB partition key (`namespace/ENTITY#{id}`). A high-traffic entity can exceed DynamoDB's per-partition throughput limits (~1,0"
tags_str: "cve, ghsa, zae-limiter"
---

<div class="incident-anchor" id="inc-09363" markdown="1">

### INC-09363  [↗](/incident/INC-09363.html)

**zae-limiter: DynamoDB hot partition throttling enables per-entity Denial of Service**  
_2026-02 · vulnerability-disclosure · Severity: Medium_

CVEs: `CVE-2026-27695`
CWEs: `CWE-770`
CVSS: **4.3** — `CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:N/I:N/A:L`

## Summary All rate limit buckets for a single entity share the same DynamoDB partition key (`namespace/ENTITY#{id}`). A high-traffic entity can exceed DynamoDB's per-partition throughput limits (~1,000 WCU/sec), causing throttling that degrades service for that entity — and potentially co-located entities in the same partition. ## Details Each `acquire()` call performs a `TransactWriteItems` (or `UpdateItem` in speculative mode) against items sharing the same partition key. For cascade entities, this doubles to 2-4 writes per request (child + parent). At sustained rates above ~500 req/sec for a single entity, DynamoDB's adaptive capacity may not redistribute fast enough, causing `ProvisionedThroughputExceededException`. The library has no built-in mitigation: - No partition key sharding/salting - No write coalescing or batching - No client-side admission control before hitting DynamoDB - `RateLimiterUnavailable` is raised but the caller has already been delayed ## Impact - **Availability**: High-traffic entities experience elevated latency and rejected requests beyond what their rate limits specify - **Fairness**: Other entities sharing the same DynamoDB partition may experience collateral throttling - **Multi-tenant risk**: In a shared LLM proxy scenario, one tenant's burst traffic could degrade service for others ## Reproduction 1. Create an entity with high rate limits (e.g., 100,000 rpm) 2. Send sustained traffic at 1,000+ req/sec to a single entity 3. Observe Dy

**Affected:** pip/zae-limiter  
**Attack vector:** `dos`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI04`  
**NIST AI RMF:** `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-27695) _(advisory)_
- [GHSA-76rv-2r9v-c5m6](https://github.com/advisories/GHSA-76rv-2r9v-c5m6) _(advisory)_
- [https://github.com/zeroae/zae-limiter/security/advisories/GHSA-76rv-2r9v-c5m6](https://github.com/zeroae/zae-limiter/security/advisories/GHSA-76rv-2r9v-c5m6) _(reference)_
- [https://github.com/zeroae/zae-limiter/commit/481ce44d818d66e31d8837bc48519660ce4c267f](https://github.com/zeroae/zae-limiter/commit/481ce44d818d66e31d8837bc48519660ce4c267f) _(reference)_
- [https://github.com/zeroae/zae-limiter/releases/tag/v0.10.1](https://github.com/zeroae/zae-limiter/releases/tag/v0.10.1) _(reference)_

**Tags:** `cve`, `ghsa`, `zae-limiter`

</div>

