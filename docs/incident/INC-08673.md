---
title: "INC-08673 — MinIO vulnerable to Path Traversal via msgpack Body in `ReadMultiple` Storage-REST Endpoint"
layout: incident
permalink: /incident/INC-08673.html
incident_id: INC-08673
year: 2026
severity: Medium
description_preview: "### Impact _What kind of vulnerability is it? Who is impacted?_ A path traversal vulnerability in MinIO's `ReadMultiple` internode storage-REST endpoint allows a caller holding the cluster root JWT to"
tags_str: "cve, ghsa, github.com-minio-minio, path-traversal"
---

{% raw %}
<div class="incident-anchor" id="inc-08673" markdown="1">

### INC-08673  [↗](/incident/INC-08673.html)

**MinIO vulnerable to Path Traversal via msgpack Body in `ReadMultiple` Storage-REST Endpoint**  
_2026-05 · vulnerability-disclosure · Severity: Medium_

CVEs: `CVE-2026-42600`
CWEs: `CWE-22`
CVSS: **4.9** — `CVSS:3.1/AV:N/AC:L/PR:H/UI:N/S:U/C:H/I:N/A:N`

### Impact _What kind of vulnerability is it? Who is impacted?_ A path traversal vulnerability in MinIO's `ReadMultiple` internode storage-REST endpoint allows a caller holding the cluster root JWT to read files from outside the configured drive roots, bounded only by the MinIO process UID. Distributed-erasure (multi-node) MinIO deployments are impacted. Single-node standalone deployments do not register the route and are not affected. The attack requires an HS512 JWT signed with `MINIO_ROOT_PASSWORD` and carrying `accessKey = MINIO_ROOT_USER` — the same secret every peer in the cluster holds to authenticate internode traffic, so a compromised peer or any actor in possession of the root credential can mint one. The `ReadMultiple` handler (`cmd/storage-rest-server.go`) decodes a msgpack `ReadMultipleReq` body containing `Bucket`, `Prefix`, and `Files` fields and forwards them to `xlStorage.ReadMultiple` (`cmd/xl-storage.go`) without validation: ```go volumeDir := pathJoin(s.drivePath, req.Bucket) // traversal resolves here for _, f := range req.Files { fullPath := pathJoin(volumeDir, req.Prefix, f) data, mt, err = s.readAllDataWithDMTime(ctx, req.Bucket, volumeDir, fullPath) } ``` `pathJoin` calls `path.Clean`, which resolves `..` components and produces an absolute path anywhere on the filesystem — it is not a root jail. The global `setRequestValidityMiddleware` rejects `..` in `r.URL.Path` and `r.Form` but does not inspect request bodies, so msgpack-en

**Affected:** go/github.com/minio/minio  
**Attack vector:** `path-traversal`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-42600) _(advisory)_
- [GHSA-xh8f-g2qw-gcm7](https://github.com/advisories/GHSA-xh8f-g2qw-gcm7) _(advisory)_
- [https://github.com/minio/minio/security/advisories/GHSA-xh8f-g2qw-gcm7](https://github.com/minio/minio/security/advisories/GHSA-xh8f-g2qw-gcm7) _(reference)_

**Tags:** `cve`, `ghsa`, `github.com-minio-minio`, `path-traversal`

</div>

{% endraw %}
