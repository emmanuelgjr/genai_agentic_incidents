---
title: "INC-08517 — pyLoad is vulnerable to stored XSS in Downloads view via unsanitized link URL in packages.js template literal"
layout: incident
permalink: /incident/INC-08517.html
incident_id: INC-08517
year: 2026
severity: High
description_preview: "## Summary The `packages.js` template at `src/pyload/webui/app/themes/modern/templates/js/packages.js:172` interpolates a stored link URL into a template literal inside single-quoted HTML and then wri"
tags_str: "cve, ghsa, pyload-ng, xss"
---

<div class="incident-anchor" id="inc-08517" markdown="1">

### INC-08517  [↗](/incident/INC-08517.html)

**pyLoad is vulnerable to stored XSS in Downloads view via unsanitized link URL in packages.js template literal**  
_2026-05 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-45348`
CWEs: `CWE-79`
CVSS: **8.7** — `CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:C/C:H/I:H/A:N`

## Summary The `packages.js` template at `src/pyload/webui/app/themes/modern/templates/js/packages.js:172` interpolates a stored link URL into a template literal inside single-quoted HTML and then writes the result to the DOM via `$(div).html(html)`. No escaping runs between the API value and `innerHTML`. An attacker (Alice) who can submit a package link puts a single quote plus event handler into the URL, breaks out of the attribute, and executes JavaScript in every operator's browser that opens the downloads view. The theme does not set a Content Security Policy that restricts inline script or event handlers. ## Details **Sink**: `src/pyload/webui/app/themes/modern/templates/js/packages.js:165-188`: ```javascript const html = ` <span class='child_status'> <span style='margin-right: 2px;color: #337ab7;' class='${link.icon}'></span> </span> <span style='font-size: 16px; font-weight: bold;'> <a onclick='return false' href='${link.url}'>${link.name}</a> </span><br/> <div class='child_secrow' ...> <span class='child_status' ...>${link.statusmsg}</span>&nbsp;${link.error}&nbsp; <span class='child_status' ...>${link.format_size}</span> <span class='child_status' ...> ${link.plugin}</span>... </div>`; const div = document.createElement("div"); $(div).attr("id", `file_${link.id}`); $(div).css("padding-left", "30px"); $(div).css("cursor", "grab"); $(div).addClass("child"); $(div).html(html); ``` `link.url` flows in from `/ap

**Affected:** pip/pyload-ng  
**Attack vector:** `xss`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-45348) _(advisory)_
- [GHSA-fcjq-435v-jx94](https://github.com/advisories/GHSA-fcjq-435v-jx94) _(advisory)_
- [https://github.com/pyload/pyload/security/advisories/GHSA-fcjq-435v-jx94](https://github.com/pyload/pyload/security/advisories/GHSA-fcjq-435v-jx94) _(reference)_

**Tags:** `cve`, `ghsa`, `pyload-ng`, `xss`

</div>

