---
title: "INC-10008 — Keras Directory Traversal Vulnerability"
layout: incident
permalink: /incident/INC-10008.html
incident_id: INC-10008
year: 2025
severity: High
description_preview: "## Summary Keras's `keras.utils.get_file()` function is vulnerable to directory traversal attacks despite implementing `filter_safe_paths()`. The vulnerability exists because `extract_archive()` uses"
tags_str: "cve, ghsa, keras, path-traversal"
---

{% raw %}
<div class="incident-anchor" id="inc-10008" markdown="1">

### INC-10008  [↗](/incident/INC-10008.html)

**Keras Directory Traversal Vulnerability**  
_2025-12 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2025-12060`
CWEs: `CWE-22`
CVSS: **9.8** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H`

## Summary Keras's `keras.utils.get_file()` function is vulnerable to directory traversal attacks despite implementing `filter_safe_paths()`. The vulnerability exists because `extract_archive()` uses Python's `tarfile.extractall()` method without the security-critical `filter="data"` parameter. A PATH_MAX symlink resolution bug occurs before path filtering, allowing malicious tar archives to bypass security checks and write files outside the intended extraction directory. ## Details ### Root Cause Analysis **Current Keras Implementation** ```python # From keras/src/utils/file_utils.py#L121 if zipfile.is_zipfile(file_path): # Zip archive. archive.extractall(path) else: # Tar archive, perhaps unsafe. Filter paths. archive.extractall(path, members=filter_safe_paths(archive)) ``` ### The Critical Flaw While Keras attempts to filter unsafe paths using `filter_safe_paths()`, this filtering happens after the tar archive members are parsed and before actual extraction. However, the PATH_MAX symlink resolution bug occurs during extraction, not during member enumeration. **Exploitation Flow:** 1. **Archive parsing**: `filter_safe_paths()` sees symlink paths that appear safe 2. **Extraction begins**: `extractall()` processes the filtered members 3. **PATH_MAX bug triggers**: Symlink resolution fails due to path length limits 4. **Security bypass**: Failed resolution causes literal path interpretation 5. **Directory traversal**: Files written outside intended direc

**Affected:** pip/keras  
**Attack vector:** `path-traversal`  

**OWASP LLM Top 10:** `LLM03`, `LLM04`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MAP-4.2`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0018`, `AML.T0020`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2025-12060) _(advisory)_
- [GHSA-hjqc-jx6g-rwp9](https://github.com/advisories/GHSA-hjqc-jx6g-rwp9) _(advisory)_
- [https://github.com/keras-team/keras/security/advisories/GHSA-hjqc-jx6g-rwp9](https://github.com/keras-team/keras/security/advisories/GHSA-hjqc-jx6g-rwp9) _(reference)_
- [https://nvd.nist.gov/vuln/detail/CVE-2025-12638](https://nvd.nist.gov/vuln/detail/CVE-2025-12638) _(reference)_
- [https://github.com/keras-team/keras/pull/21760](https://github.com/keras-team/keras/pull/21760) _(reference)_
- [https://github.com/keras-team/keras/commit/47fcb397ee4caffd5a75efd1fa3067559594e951](https://github.com/keras-team/keras/commit/47fcb397ee4caffd5a75efd1fa3067559594e951) _(reference)_

**Tags:** `cve`, `ghsa`, `keras`, `path-traversal`

</div>

{% endraw %}
