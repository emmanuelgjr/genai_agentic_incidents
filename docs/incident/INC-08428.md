---
title: "INC-08428 — Twig: Sandbox property allowlist bypass via the `column` filter (array_column on objects)"
layout: incident
permalink: /incident/INC-08428.html
incident_id: INC-08428
year: 2026
severity: Low
description_preview: "### Description The `column` filter passes its input straight to PHP's native `array_column()`. When the array elements are objects, `array_column()` reads `$obj->$name` (and `$obj->$index`) directly,"
tags_str: "cve, ghsa, twig-twig"
---

{% raw %}
<div class="incident-anchor" id="inc-08428" markdown="1">

### INC-08428  [↗](/incident/INC-08428.html)

**Twig: Sandbox property allowlist bypass via the `column` filter (array_column on objects)**  
_2026-05 · vulnerability-disclosure · Severity: Low_

CVEs: `CVE-2026-46635`
CWEs: `CWE-863`

### Description The `column` filter passes its input straight to PHP's native `array_column()`. When the array elements are objects, `array_column()` reads `$obj->$name` (and `$obj->$index`) directly, including invoking `__get`/`__isset`. Because this property read happens entirely in PHP native code and never reaches `CoreExtension::getAttribute()`, `SandboxExtension::checkPropertyAllowed()` is never consulted. An untrusted template author with `column` in their `allowedFilters` list can therefore read any public or magic property of any object reachable in the render context, regardless of the `SecurityPolicy` `allowedProperties` list. This is a variant of CVE-2024-51755 / GHSA-jjxq-ff2g-95vh that the `ArrayAccess`-focused fix did not cover. ### Resolution The `column` filter now routes object property reads through the sandbox-aware attribute accessor so the property allowlist is enforced. ### Credits Twig: would like to thank Claude Mythos Preview (via Project Glasswing) for reporting the issue and providing the fix.

**Affected:** composer/twig/twig  
**Attack vector:** `other`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI04`  
**NIST AI RMF:** `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-46635) _(advisory)_
- [GHSA-vcc8-phrv-43wj](https://github.com/advisories/GHSA-vcc8-phrv-43wj) _(advisory)_
- [https://github.com/twigphp/Twig/security/advisories/GHSA-vcc8-phrv-43wj](https://github.com/twigphp/Twig/security/advisories/GHSA-vcc8-phrv-43wj) _(reference)_
- [https://github.com/FriendsOfPHP/security-advisories/blob/master/twig/twig/CVE-2026-46635.yaml](https://github.com/FriendsOfPHP/security-advisories/blob/master/twig/twig/CVE-2026-46635.yaml) _(reference)_
- [https://symfony.com/cve-2026-46635](https://symfony.com/cve-2026-46635) _(reference)_

**Tags:** `cve`, `ghsa`, `twig-twig`

</div>

{% endraw %}
