---
title: "INC-09219 — Unhead has XSS bypass in `useHeadSafe` via attribute name injection and case-sensitive protocol check"
layout: incident
permalink: /incident/INC-09219.html
incident_id: INC-09219
year: 2026
severity: Medium
description_preview: "## Summary `useHeadSafe()` can be bypassed to inject arbitrary HTML attributes, including event handlers, into SSR-rendered `<head>` tags. This is the composable that Nuxt docs recommend for safely ha"
tags_str: "cve, ghsa, unhead, xss"
---

{% raw %}
<div class="incident-anchor" id="inc-09219" markdown="1">

### INC-09219  [↗](/incident/INC-09219.html)

**Unhead has XSS bypass in `useHeadSafe` via attribute name injection and case-sensitive protocol check**  
_2026-03 · vulnerability-disclosure · Severity: Medium_

CVEs: `CVE-2026-31860`
CWEs: `CWE-79`

## Summary `useHeadSafe()` can be bypassed to inject arbitrary HTML attributes, including event handlers, into SSR-rendered `<head>` tags. This is the composable that Nuxt docs recommend for safely handling user-generated content. ## Details **XSS via `data-*` attribute name injection** The `acceptDataAttrs` function (safe.ts, line 16-20) allows any property key starting with `data-` through to the final HTML. It only checks the prefix, not whether the key contains spaces or other characters that break HTML attribute parsing. ```typescript function acceptDataAttrs(value: Record<string, string>) { return Object.fromEntries( Object.entries(value || {}).filter(([key]) => key === 'id' || key.startsWith('data-')), ) } ``` This result gets merged into every tag's props at line 114: ```typescript tag.props = { ...acceptDataAttrs(prev), ...next } ``` Then `propsToString` (propsToString.ts, line 26) interpolates property keys directly into the HTML string with no sanitization: ```typescript attrs += value === true ? ` ${key}` : ` ${key}="${encodeAttribute(value)}"` ``` A space in the key breaks out of the attribute name. Everything after the space becomes separate HTML attributes. ### PoC The most practical vector uses a `link` tag. `<link rel="stylesheet">` fires `onload` once the stylesheet loads, giving reliable script execution: ```javascript useHeadSafe({ link: [{ rel: 'stylesheet', href: '/valid-stylesheet.css', 'data-x onload=alert(document.do

**Affected:** npm/unhead  
**Attack vector:** `xss`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-31860) _(advisory)_
- [GHSA-g5xx-pwrp-g3fv](https://github.com/advisories/GHSA-g5xx-pwrp-g3fv) _(advisory)_
- [https://github.com/unjs/unhead/security/advisories/GHSA-g5xx-pwrp-g3fv](https://github.com/unjs/unhead/security/advisories/GHSA-g5xx-pwrp-g3fv) _(reference)_
- [https://github.com/unjs/unhead/commit/9ecc4f9568b0e23938f36d4b23fcfa4a18a89045](https://github.com/unjs/unhead/commit/9ecc4f9568b0e23938f36d4b23fcfa4a18a89045) _(reference)_
- [https://github.com/unjs/unhead/releases/tag/v2.1.11](https://github.com/unjs/unhead/releases/tag/v2.1.11) _(reference)_

**Tags:** `cve`, `ghsa`, `unhead`, `xss`

</div>

{% endraw %}
