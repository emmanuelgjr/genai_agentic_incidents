---
title: "INC-11826 — PostgreSQL JDBC Driver SQL Injection in ResultSet.refreshRow() with malicious column names"
layout: incident
permalink: /incident/INC-11826.html
incident_id: INC-11826
year: 2022
severity: High
description_preview: "### Impact _What kind of vulnerability is it? Who is impacted?_ The PGJDBC implementation of the `java.sql.ResultRow.refreshRow()` method is not performing escaping of column names so a malicious colu"
tags_str: "cve, ghsa, org.postgresql-postgresql, sql-injection"
---

{% raw %}
<div class="incident-anchor" id="inc-11826" markdown="1">

### INC-11826  [↗](/incident/INC-11826.html)

**PostgreSQL JDBC Driver SQL Injection in ResultSet.refreshRow() with malicious column names**  
_2022-08 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2022-31197`
CWEs: `CWE-89`
CVSS: **7.1** — `CVSS:3.1/AV:N/AC:H/PR:L/UI:R/S:U/C:H/I:H/A:H`

### Impact _What kind of vulnerability is it? Who is impacted?_ The PGJDBC implementation of the `java.sql.ResultRow.refreshRow()` method is not performing escaping of column names so a malicious column name that contains a statement terminator, e.g. `;`, could lead to SQL injection. This could lead to executing additional SQL commands as the application's JDBC user. User applications that do not invoke the `ResultSet.refreshRow()` method are not impacted. User application that do invoke that method are impacted if the underlying database that they are querying via their JDBC application may be under the control of an attacker. The attack requires the attacker to trick the user into executing SQL against a table name who's column names would contain the malicious SQL and subsequently invoke the `refreshRow()` method on the ResultSet. For example: ```sql CREATE TABLE refresh_row_example ( id int PRIMARY KEY, "1 FROM refresh_row_example; SELECT pg_sleep(10); SELECT * " int ); ``` This example has a table with two columns. The name of the second column is crafted to contain a statement terminator followed by additional SQL. Invoking the `ResultSet.refreshRow()` on a ResultSet that queried this table, e.g. `SELECT * FROM refresh_row`, would cause the additional SQL commands such as the `SELECT pg_sleep(10)` invocation to be executed. As the multi statement command would contain multiple results, it would not be possible for the attacker to get data directly out of

**Affected:** maven/org.postgresql:postgresql, maven/org.postgresql:postgresql, maven/org.postgresql:postgresql  
**Attack vector:** `sql-injection`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2022-31197) _(advisory)_
- [GHSA-r38f-c4h4-hqq2](https://github.com/advisories/GHSA-r38f-c4h4-hqq2) _(advisory)_
- [https://github.com/pgjdbc/pgjdbc/security/advisories/GHSA-r38f-c4h4-hqq2](https://github.com/pgjdbc/pgjdbc/security/advisories/GHSA-r38f-c4h4-hqq2) _(reference)_
- [https://github.com/pgjdbc/pgjdbc/commit/739e599d52ad80f8dcd6efedc6157859b1a9d637](https://github.com/pgjdbc/pgjdbc/commit/739e599d52ad80f8dcd6efedc6157859b1a9d637) _(reference)_
- [https://lists.debian.org/debian-lts-announce/2022/10/msg00009.html](https://lists.debian.org/debian-lts-announce/2022/10/msg00009.html) _(reference)_
- [https://lists.debian.org/debian-lts-announce/2024/12/msg00017.html](https://lists.debian.org/debian-lts-announce/2024/12/msg00017.html) _(reference)_

**Tags:** `cve`, `ghsa`, `org.postgresql-postgresql`, `sql-injection`

</div>

{% endraw %}
