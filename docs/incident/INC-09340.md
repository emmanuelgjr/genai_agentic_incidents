---
title: "INC-09340 — Junrar has an arbitrary file write due to backslash Path Traversal bypass in LocalFolderExtractor on Linux/Unix"
layout: incident
permalink: /incident/INC-09340.html
incident_id: INC-09340
year: 2026
severity: Medium
description_preview: "### Summary A backslash path traversal vulnerability in `LocalFolderExtractor` allows an attacker to write arbitrary files with attacker-controlled content anywhere on the filesystem when a crafted RA"
tags_str: "com.github.junrar-junrar, cve, ghsa, path-traversal"
---

<div class="incident-anchor" id="inc-09340" markdown="1">

### INC-09340  [↗](/incident/INC-09340.html)

**Junrar has an arbitrary file write due to backslash Path Traversal bypass in LocalFolderExtractor on Linux/Unix**  
_2026-02 · vulnerability-disclosure · Severity: Medium_

CVEs: `CVE-2026-28208`
CWEs: `CWE-22`
CVSS: **5.9** — `CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:N/I:H/A:N`

### Summary A backslash path traversal vulnerability in `LocalFolderExtractor` allows an attacker to write arbitrary files with attacker-controlled content anywhere on the filesystem when a crafted RAR archive is extracted on Linux/Unix. This can often lead to remote code execution (e.g., overwriting shell profiles, source code, cron jobs, etc). ### Details The `createFile()` method in [`LocalFolderExtractor.java`](https://github.com/junrar/junrar/blob/master/src/main/java/com/github/junrar/LocalFolderExtractor.java) validates extraction paths using `getCanonicalPath().startsWith()` to ensure files stay within the destination directory: ```java File f = new File(destination, name); String dirCanonPath = f.getCanonicalPath(); if (!dirCanonPath.startsWith(destination.getCanonicalPath())) { throw new IllegalStateException("Rar contains file with invalid path: '" + dirCanonPath + "'"); } ``` On Linux/Unix, backslashes are literal filename characters, not path separators. A RAR entry named `..\..\tmp\evil.txt` is treated by `getCanonicalPath()` as a single literal filename containing backslash characters — no `..` resolution occurs, and the `startsWith` check passes. However, `makeFile()` then splits the filename on backslashes and reconstructs the path using the platform's file separator: ```java final String[] dirs = name.split("\\\\"); // dirs = ["..", "..", "tmp", "evil.txt"] // ... path = path + File.separator + dirs[i]; // File.separator is "/" on Linux ``` This

**Affected:** maven/com.github.junrar:junrar  
**Attack vector:** `path-traversal`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-28208) _(advisory)_
- [GHSA-j273-m5qq-6825](https://github.com/advisories/GHSA-j273-m5qq-6825) _(advisory)_
- [https://github.com/junrar/junrar/security/advisories/GHSA-j273-m5qq-6825](https://github.com/junrar/junrar/security/advisories/GHSA-j273-m5qq-6825) _(reference)_
- [https://github.com/junrar/junrar/commit/947ff1d33f00f940aa68ae2593500291d799d954](https://github.com/junrar/junrar/commit/947ff1d33f00f940aa68ae2593500291d799d954) _(reference)_
- [https://github.com/junrar/junrar/releases/tag/v7.5.8](https://github.com/junrar/junrar/releases/tag/v7.5.8) _(reference)_

**Tags:** `com.github.junrar-junrar`, `cve`, `ghsa`, `path-traversal`

</div>

