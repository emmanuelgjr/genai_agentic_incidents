---
title: "INC-01643 — PinchTab — Auth Bypass (CVE-2026-33622)"
layout: incident
permalink: /incident/INC-01643.html
incident_id: INC-01643
year: 2026
severity: High
description_preview: "PinchTab is a standalone HTTP server that gives AI agents direct control over a Chrome browser. PinchTab `v0.8.3` through `v0.8.5` allow arbitrary JavaScript execution through `POST /wait` and `POST /"
tags_str: "auth-bypass, cve, ghsa, github.com-pinchtab-pinchtab, github.com-pinchtab-pinchtab-cmd-pinchtab, nvd"
---

{% raw %}
<div class="incident-anchor" id="inc-01643" markdown="1">

### INC-01643  [↗](/incident/INC-01643.html)

**PinchTab — Auth Bypass (CVE-2026-33622)**  
_2026-03 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-33622`
CWEs: `CWE-284`, `CWE-693`, `CWE-94`
CVSS: **8.8** — `CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:H/I:H/A:H`

PinchTab is a standalone HTTP server that gives AI agents direct control over a Chrome browser. PinchTab `v0.8.3` through `v0.8.5` allow arbitrary JavaScript execution through `POST /wait` and `POST /tabs/{id}/wait` when the request uses `fn` mode, even if `security.allowEvaluate` is disabled. `POST /evaluate` correctly enforces the `security.allowEvaluate` guard, which is disabled by default. However, in the affected releases, `POST /wait` accepted a user-controlled `fn` expression, embedded it directly into executable JavaScript, and evaluated it in the browser context without checking the same policy. This is a security-policy bypass rather than a separate authentication bypass. Exploitation still requires authenticated API access, but a caller with the server token can execute arbitrary JavaScript in a tab context even when the operator explicitly disabled JavaScript evaluation. The current worktree fixes this by applying the same policy boundary to `fn` mode in `/wait` that already exists on `/evaluate`, while preserving the non-code wait modes. As of time of publication, a patched version is not yet available.

**Affected:** pinchtab/pinchtab  
**Attack vector:** `auth-bypass`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-33622) _(advisory)_
- [security-advisories@github.com](https://github.com/pinchtab/pinchtab/security/advisories/GHSA-w5pc-m664-r62v) _(vendor)_
- [GHSA-w5pc-m664-r62v](https://github.com/advisories/GHSA-w5pc-m664-r62v) _(advisory)_

**Tags:** `auth-bypass`, `cve`, `ghsa`, `github.com-pinchtab-pinchtab`, `github.com-pinchtab-pinchtab-cmd-pinchtab`, `nvd`

</div>

{% endraw %}
