---
title: "INC-09708 — Anyquery has Path Traversal through `clear_plugin_cache`, Allowing Arbitrary Directory Deletion"
layout: incident
permalink: /incident/INC-09708.html
incident_id: INC-09708
year: 2026
severity: High
description_preview: "# Path Traversal in `clear_plugin_cache` Allows Arbitrary Directory Deletion | Field | Value | | ---------------- | ----- | | Repository | julien040/anyquery | | Affected version | 0.4.4 | | Vulnerabi"
tags_str: "cve, ghsa, github.com-julien040-anyquery, path-traversal"
---

{% raw %}
<div class="incident-anchor" id="inc-09708" markdown="1">

### INC-09708  [↗](/incident/INC-09708.html)

**Anyquery has Path Traversal through `clear_plugin_cache`, Allowing Arbitrary Directory Deletion**  
_2026-06 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-47253`
CWEs: `CWE-22`
CVSS: **7.3** — `CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:U/C:N/I:H/A:H`

# Path Traversal in `clear_plugin_cache` Allows Arbitrary Directory Deletion | Field | Value | | ---------------- | ----- | | Repository | julien040/anyquery | | Affected version | 0.4.4 | | Vulnerability | CWE-22 — Improper Limitation of a Pathname to a Restricted Directory | | Severity | High | ## Summary The SQL scalar function `clear_plugin_cache(plugin)` in `namespace/other_functions.go` passes the caller-supplied `plugin` argument directly to `path.Join` and then to `os.RemoveAll`, with only an empty-string check as a guard. Because `path.Join` silently resolves `..` segments, a low-privileged bearer-token holder can submit `SELECT clear_plugin_cache('../../../../tmp/target')` to the `/v1/query` HTTP endpoint and delete any directory reachable by the server process. In the verified scenario, a directory outside `$XDG_CACHE_HOME/anyquery/plugins/` was successfully deleted, confirming full path-traversal exploitation. ## Affected Code `namespace/other_functions.go:46` — `pathlib.Join` resolves `..` segments in attacker-controlled `plugin`, producing a path outside the cache root `namespace/other_functions.go:53` — `os.RemoveAll` unconditionally deletes the traversed path ```go func clear_plugin_cache(plugin string) string { pathToRemove := pathlib.Join(xdg.CacheHome, "anyquery", "plugins", plugin) if plugin == "" { return "The plugin name is empty" } // Remove the directory err := os.RemoveAll(pathToRemove) if err != nil { r

**Affected:** go/github.com/julien040/anyquery  
**Attack vector:** `path-traversal`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-47253) _(advisory)_
- [GHSA-j9rx-rppg-6hh4](https://github.com/advisories/GHSA-j9rx-rppg-6hh4) _(advisory)_
- [https://github.com/julien040/anyquery/security/advisories/GHSA-j9rx-rppg-6hh4](https://github.com/julien040/anyquery/security/advisories/GHSA-j9rx-rppg-6hh4) _(reference)_
- [https://github.com/julien040/anyquery/releases/tag/0.4.5](https://github.com/julien040/anyquery/releases/tag/0.4.5) _(reference)_

**Tags:** `cve`, `ghsa`, `github.com-julien040-anyquery`, `path-traversal`

</div>

{% endraw %}
