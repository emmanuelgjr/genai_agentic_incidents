---
title: "INC-09091 — yaml is vulnerable to Stack Overflow via deeply nested YAML collections"
layout: incident
permalink: /incident/INC-09091.html
incident_id: INC-09091
year: 2026
severity: Medium
description_preview: "Parsing a YAML document with `yaml` may throw a RangeError due to a stack overflow. The node resolution/composition phase uses recursive function calls without a depth bound. An attacker who can suppl"
tags_str: "cve, ghsa, yaml"
---

<div class="incident-anchor" id="inc-09091" markdown="1">

### INC-09091  [↗](/incident/INC-09091.html)

**yaml is vulnerable to Stack Overflow via deeply nested YAML collections**  
_2026-03 · vulnerability-disclosure · Severity: Medium_

CVEs: `CVE-2026-33532`
CWEs: `CWE-674`
CVSS: **4.3** — `CVSS:3.1/AV:N/AC:L/PR:L/UI:N/S:U/C:N/I:N/A:L`

Parsing a YAML document with `yaml` may throw a RangeError due to a stack overflow. The node resolution/composition phase uses recursive function calls without a depth bound. An attacker who can supply YAML for parsing can trigger a `RangeError: Maximum call stack size exceeded` with a small payload (~2–10 KB). The `RangeError` is not a `YAMLParseError`, so applications that only catch YAML-specific errors will encounter an unexpected exception type. Depending on the host application's exception handling, this can fail requests or terminate the Node.js process. Flow sequences allow deep nesting with minimal bytes (2 bytes per level: one `[` and one `]`). On the default Node.js stack, approximately 1,000–5,000 levels of nesting (2–10 KB input) exhaust the call stack. The exact threshold is environment-dependent (Node.js version, stack size, call stack depth at invocation). Note: the library's `Parser` (CST phase) uses a stack-based iterative approach and is not affected. Only the compose/resolve phase uses actual call-stack recursion. All three public parsing APIs are affected: `YAML.parse()`, `YAML.parseDocument()`, and `YAML.parseAllDocuments()`. ### PoC ```javascript const YAML = require('yaml'); // ~10 KB payload: 5000 levels of nested flow sequences const payload = '['.repeat(5000) + '1' + ']'.repeat(5000); try { YAML.parse(payload); } catch (e) { console.log(e.constructor.name); // RangeError (NOT YAMLParseError) console.log(e.message); // Maximum

**Affected:** npm/yaml, npm/yaml  
**Attack vector:** `other`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI04`  
**NIST AI RMF:** `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-33532) _(advisory)_
- [GHSA-48c2-rrv3-qjmp](https://github.com/advisories/GHSA-48c2-rrv3-qjmp) _(advisory)_
- [https://github.com/eemeli/yaml/security/advisories/GHSA-48c2-rrv3-qjmp](https://github.com/eemeli/yaml/security/advisories/GHSA-48c2-rrv3-qjmp) _(reference)_
- [https://github.com/eemeli/yaml/commit/1e84ebbea7ec35011a4c61bbb820a529ee4f359b](https://github.com/eemeli/yaml/commit/1e84ebbea7ec35011a4c61bbb820a529ee4f359b) _(reference)_
- [https://github.com/eemeli/yaml/releases/tag/v1.10.3](https://github.com/eemeli/yaml/releases/tag/v1.10.3) _(reference)_
- [https://github.com/eemeli/yaml/releases/tag/v2.8.3](https://github.com/eemeli/yaml/releases/tag/v2.8.3) _(reference)_

**Tags:** `cve`, `ghsa`, `yaml`

</div>

