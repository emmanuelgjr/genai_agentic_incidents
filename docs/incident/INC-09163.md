---
title: "INC-09163 — mo has a XSS via inline SVG script tags in Markdown rendering"
layout: incident
permalink: /incident/INC-09163.html
incident_id: INC-09163
year: 2026
severity: Low
description_preview: "### Summary When rendering Markdown files containing inline SVG elements with `<script>` tags, the embedded JavaScript is executed in the browser. This is due to `rehype-raw` passing raw HTML (includi"
tags_str: "ghsa, github.com-k1low-mo"
---

{% raw %}
<div class="incident-anchor" id="inc-09163" markdown="1">

### INC-09163  [↗](/incident/INC-09163.html)

**mo has a XSS via inline SVG script tags in Markdown rendering**  
_2026-03 · vulnerability-disclosure · Severity: Low_
CWEs: `CWE-79`

### Summary When rendering Markdown files containing inline SVG elements with `<script>` tags, the embedded JavaScript is executed in the browser. This is due to `rehype-raw` passing raw HTML (including SVG) through to the DOM without sanitization. ### PoC ```html <svg><script>alert(1)</script></svg> ``` Embedding the above in a Markdown file opened with mo causes the script to execute. ### Impact Arbitrary JavaScript execution in the context of the mo application when a user opens a malicious Markdown file. This could lead to data exfiltration of other opened Markdown file contents. ### Remediation Fixed by adding `rehype-sanitize` to the Markdown rendering pipeline, which strips dangerous elements such as `<script>` while preserving safe HTML.

**Affected:** go/github.com/k1LoW/mo  
**Attack vector:** `xss`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI04`  
**NIST AI RMF:** `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`  

**References:**
- [GHSA-vccx-p757-pv6h](https://github.com/advisories/GHSA-vccx-p757-pv6h) _(advisory)_
- [https://github.com/k1LoW/mo/security/advisories/GHSA-vccx-p757-pv6h](https://github.com/k1LoW/mo/security/advisories/GHSA-vccx-p757-pv6h) _(reference)_

**Tags:** `ghsa`, `github.com-k1low-mo`

</div>

{% endraw %}
