---
title: "INC-08975 — Signal K Server: Arbitrary Prototype Read via `from` Field Bypass"
layout: incident
permalink: /incident/INC-08975.html
incident_id: INC-08975
year: 2026
severity: Low
description_preview: "## Summary The /signalk/v1/applicationData/... JSON-patch endpoint allows users to modify stored application data. To prevent Prototype Pollution, the developers implemented an isPrototypePollutionPat"
tags_str: "cve, ghsa, rce, signalk-server"
---

{% raw %}
<div class="incident-anchor" id="inc-08975" markdown="1">

### INC-08975  [↗](/incident/INC-08975.html)

**Signal K Server: Arbitrary Prototype Read via `from` Field Bypass**  
_2026-04 · vulnerability-disclosure · Severity: Low_

CVEs: `CVE-2026-35038`
CWEs: `CWE-125`, `CWE-20`, `CWE-200`

## Summary The /signalk/v1/applicationData/... JSON-patch endpoint allows users to modify stored application data. To prevent Prototype Pollution, the developers implemented an isPrototypePollutionPath guard. However, this guard only checks the path property of incoming JSON-patch objects. It completely fails to check the from property. Because JSON-patch operations like copy and move extract data using the from property path, an attacker can construct a payload where from targets /__proto__/someProperty, completely evading the security check and successfully executing an Arbitrary Prototype Read. While this does not allow arbitrary code execution (as the destination path remains protected from __proto__), it does allow a user to exfiltrate internal Node functions and prototype state into their own application data. ## Vulnerability Root Cause File: src/interfaces/applicationData.js (Lines 48-57) ``` const DANGEROUS_PATH_SEGMENTS = ['__proto__', 'constructor', 'prototype'] function isPrototypePollutionPath(pathString) { const segments = pathString.split(/[./]/) return segments.some((seg) => DANGEROUS_PATH_SEGMENTS.includes(seg)) } function hasPrototypePollutionPatch(patches) { return patches.some( // [!VULNERABLE] Only checks patch.path, completely ignores patch.from (patch) => patch.path && isPrototypePollutionPath(patch.path) ) } ``` At Line 201: ``` if (hasPrototypePollutionPatch(req.body)) { res.status(400).send('invalid patch path') return }

**Affected:** npm/signalk-server  
**Attack vector:** `rce`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-35038) _(advisory)_
- [GHSA-qh3j-mrg8-f234](https://github.com/advisories/GHSA-qh3j-mrg8-f234) _(advisory)_
- [https://github.com/SignalK/signalk-server/security/advisories/GHSA-qh3j-mrg8-f234](https://github.com/SignalK/signalk-server/security/advisories/GHSA-qh3j-mrg8-f234) _(reference)_
- [https://github.com/SignalK/signalk-server/releases/tag/v2.24.0](https://github.com/SignalK/signalk-server/releases/tag/v2.24.0) _(reference)_

**Tags:** `cve`, `ghsa`, `rce`, `signalk-server`

</div>

{% endraw %}
