---
title: "INC-08534 — CoreShop Vulnerable to Remote Code Execution (RCE) via Insecure `pull_request_target` Configuration"
layout: incident
permalink: /incident/INC-08534.html
incident_id: INC-08534
year: 2026
severity: High
description_preview: "### Summary The GitHub Actions workflow (`.github/workflows/static.yml`) uses the `pull_request_target` trigger but dangerously checks out the unverified code from the pull request head (`ref: ${{ git"
tags_str: "coreshop-core-shop, cve, ghsa, rce"
---

<div class="incident-anchor" id="inc-08534" markdown="1">

### INC-08534  [↗](/incident/INC-08534.html)

**CoreShop Vulnerable to Remote Code Execution (RCE) via Insecure `pull_request_target` Configuration**  
_2026-05 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-41249`
CWEs: `CWE-94`
CVSS: **8.2** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:L/A:N`

### Summary The GitHub Actions workflow (`.github/workflows/static.yml`) uses the `pull_request_target` trigger but dangerously checks out the unverified code from the pull request head (`ref: ${{ github.event.pull_request.head.ref }}`). Subsequently, it executes a script (`bin/console`) from this untrusted checkout. This allows any external attacker to achieve Remote Code Execution (RCE) on the GitHub Actions runner simply by submitting a malicious Pull Request. Also known as a "Pwn Request" vulnerability. **Steps to Reproduce:** 1. Fork the target repository. 2. In the forked repository, modify a file that satisfies the `paths` condition (e.g., `src/dummy.php` or `composer.json`) to trigger the workflow. 3. Modify the `bin/console` file (which is executed in the workflow steps) with the following malicious payload: ```bash #!/bin/bash echo "=== PWNED ===" echo "whoami:" whoami ``` 4. Commit the changes and open a Pull Request against the `5.0` or `next` branch of the base repository. 5. The `Static Tests` workflow will trigger automatically. Navigate to the Actions tab and inspect the logs for the `Validate YAML` (or any step executing `bin/console`). 6. You will see the output of `whoami` (typically `runner`), proving that the arbitrary code was successfully executed in the runner's context. <img width="490" height="87" alt="スクリーンショット 2026-04-14 11 14 56" src="https://github.com/user-attachments/assets/94276033-b989-46dc-b4a1-3dafa1603235" /> **Impact:** Because `pull

**Affected:** composer/coreshop/core-shop  
**Attack vector:** `rce`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-41249) _(advisory)_
- [GHSA-q58j-g3f4-h26h](https://github.com/advisories/GHSA-q58j-g3f4-h26h) _(advisory)_
- [https://github.com/coreshop/CoreShop/security/advisories/GHSA-q58j-g3f4-h26h](https://github.com/coreshop/CoreShop/security/advisories/GHSA-q58j-g3f4-h26h) _(reference)_
- [https://github.com/coreshop/CoreShop/commit/cc1e3f547228ec5ebfc1dc0472f9a3cc5f4137a4](https://github.com/coreshop/CoreShop/commit/cc1e3f547228ec5ebfc1dc0472f9a3cc5f4137a4) _(reference)_
- [https://github.com/coreshop/CoreShop/blob/5.1.0-beta.1/.github/workflows/static.yml#L14](https://github.com/coreshop/CoreShop/blob/5.1.0-beta.1/.github/workflows/static.yml#L14) _(reference)_

**Tags:** `coreshop-core-shop`, `cve`, `ghsa`, `rce`

</div>

