---
title: "INC-08646 — Micronaut has Unbounded `bundleCache` in `ResourceBundleMessageSource` that Allows Memory Exhaustion via `Accept-Language` Header"
layout: incident
permalink: /incident/INC-08646.html
incident_id: INC-08646
year: 2026
severity: Low
description_preview: "## Summary `ResourceBundleMessageSource` maintains two caches: `messageCache` (bounded at 100 entries via `ConcurrentLinkedHashMap`) and `bundleCache` (unbounded `ConcurrentHashMap`). The `bundleCache"
tags_str: "cve, ghsa, io.micronaut-micronaut-inject"
---

{% raw %}
<div class="incident-anchor" id="inc-08646" markdown="1">

### INC-08646  [↗](/incident/INC-08646.html)

**Micronaut has Unbounded `bundleCache` in `ResourceBundleMessageSource` that Allows Memory Exhaustion via `Accept-Language` Header**  
_2026-05 · vulnerability-disclosure · Severity: Low_

CVEs: `CVE-2026-44242`
CWEs: `CWE-400`
CVSS: **3.7** — `CVSS:3.1/AV:N/AC:H/PR:N/UI:N/S:U/C:N/I:N/A:L`

## Summary `ResourceBundleMessageSource` maintains two caches: `messageCache` (bounded at 100 entries via `ConcurrentLinkedHashMap`) and `bundleCache` (unbounded `ConcurrentHashMap`). The `bundleCache` is keyed by `(Locale, baseName)` where the locale originates from the HTTP `Accept-Language` header. In applications that explicitly register a `ResourceBundleMessageSource` bean and serve HTML error responses, an unauthenticated attacker can exhaust heap memory by sending requests with large numbers of unique `Accept-Language` values, each causing a new entry in the unbounded `bundleCache`. Unlike GHSA-2hcp-gjrf-7fhc and the sibling `messageCache` (both bounded), `bundleCache` was not updated to use a bounded cache implementation. ## Details The `bundleCache` is initialized in `inject/src/main/java/io/micronaut/context/i18n/ResourceBundleMessageSource.java` at line 150: ```java // ResourceBundleMessageSource.java:139-152 protected Map<MessageKey, Optional<String>> buildMessageCache() { return new ConcurrentLinkedHashMap.Builder<MessageKey, Optional<String>>() .maximumWeightedCapacity(100) // ← BOUNDED ✓ .build(); } protected Map<MessageKey, Optional<ResourceBundle>> buildBundleCache() { return new ConcurrentHashMap<>(18); // ← UNBOUNDED ✗ } ``` The `resolveBundle()` method at line 169 inserts into `bundleCache` with no eviction policy: ```java // ResourceBundleMessageSource.java:169-185 private Optional<ResourceBundle> resolveBund

**Affected:** maven/io.micronaut:micronaut-inject  
**Attack vector:** `other`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI04`  
**NIST AI RMF:** `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-44242) _(advisory)_
- [GHSA-3rfq-4wpf-qqw3](https://github.com/advisories/GHSA-3rfq-4wpf-qqw3) _(advisory)_
- [https://github.com/micronaut-projects/micronaut-core/security/advisories/GHSA-3rfq-4wpf-qqw3](https://github.com/micronaut-projects/micronaut-core/security/advisories/GHSA-3rfq-4wpf-qqw3) _(reference)_
- [https://github.com/micronaut-projects/micronaut-core/releases/tag/v4.10.22](https://github.com/micronaut-projects/micronaut-core/releases/tag/v4.10.22) _(reference)_

**Tags:** `cve`, `ghsa`, `io.micronaut-micronaut-inject`

</div>

{% endraw %}
