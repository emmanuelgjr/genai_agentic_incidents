---
title: "INC-08980 — Ech0 has Unauthenticated Server-Side Request Forgery in Website Preview Feature"
layout: incident
permalink: /incident/INC-08980.html
incident_id: INC-08980
year: 2026
severity: High
description_preview: "### Summary Ech0 implements **link preview** (editor fetches a page title) through **`GET /api/website/title`**. That is **legitimate product behavior**, but the implementation is **unsafe**: the rout"
tags_str: "cve, ghsa, github.com-lin-snow-ech0, ssrf"
---

{% raw %}
<div class="incident-anchor" id="inc-08980" markdown="1">

### INC-08980  [↗](/incident/INC-08980.html)

**Ech0 has Unauthenticated Server-Side Request Forgery in Website Preview Feature**  
_2026-04 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-35036`
CWEs: `CWE-918`
CVSS: **7.5** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:N/A:N`

### Summary Ech0 implements **link preview** (editor fetches a page title) through **`GET /api/website/title`**. That is **legitimate product behavior**, but the implementation is **unsafe**: the route is **unauthenticated**, accepts a **fully attacker-controlled URL**, performs a **server-side GET**, reads the **entire response body** into memory (`io.ReadAll`). There is **no** host allowlist, **no** SSRF filter, and **`InsecureSkipVerify: true`** on the outbound client. **Attacker outcome :** Anyone who can reach the instance can **force the Ech0 server** to open **HTTP/HTTPS URLs of their choice** as seen from the **server’s network position** (Docker bridge, VPC, localhost from the process view). Go’s default `http.Client` **follows redirects** (unless disabled). Redirect chains can move the server-side request from an allowed-looking host to an internal target; the code does not disable this in `SendRequest`. ### Affected Components **Ech0 codebase:** - `internal/handler/common/common.go` Handles the `/api/website/title` endpoint and accepts user-controlled URL input. - `internal/service/common/common.go` Processes the request and invokes the outbound HTTP fetch (`GetWebsiteTitle`). - `internal/util/http/http.go` Performs the HTTP request (`SendRequest`) with the following insecure configurations: - No URL validation or allowlist - Redirects enabled (default client behavior) - `InsecureSkipVerify: true` ### PoC **Environment:** Ech0 listening

**Affected:** go/github.com/lin-snow/ech0  
**Attack vector:** `ssrf`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0049`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-35036) _(advisory)_
- [GHSA-wc4h-2348-jc3p](https://github.com/advisories/GHSA-wc4h-2348-jc3p) _(advisory)_
- [https://github.com/lin-snow/Ech0/security/advisories/GHSA-wc4h-2348-jc3p](https://github.com/lin-snow/Ech0/security/advisories/GHSA-wc4h-2348-jc3p) _(reference)_

**Tags:** `cve`, `ghsa`, `github.com-lin-snow-ech0`, `ssrf`

</div>

{% endraw %}
