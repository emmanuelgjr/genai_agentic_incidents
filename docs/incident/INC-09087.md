---
title: "INC-09087 — AVideo: Unauthenticated CDN Configuration Takeover via Empty Default Key Bypass and Mass-Assignment"
layout: incident
permalink: /incident/INC-09087.html
incident_id: INC-09087
year: 2026
severity: High
description_preview: "## Summary The CDN plugin endpoints `plugin/CDN/status.json.php` and `plugin/CDN/disable.json.php` use key-based authentication with an empty string default key. When the CDN plugin is enabled but the"
tags_str: "cve, ghsa, path-traversal, wwbn-avideo"
---

<div class="incident-anchor" id="inc-09087" markdown="1">

### INC-09087  [↗](/incident/INC-09087.html)

**AVideo: Unauthenticated CDN Configuration Takeover via Empty Default Key Bypass and Mass-Assignment**  
_2026-03 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-33719`
CWEs: `CWE-306`
CVSS: **8.6** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:L/I:H/A:L`

## Summary The CDN plugin endpoints `plugin/CDN/status.json.php` and `plugin/CDN/disable.json.php` use key-based authentication with an empty string default key. When the CDN plugin is enabled but the key has not been configured (the default state), the key validation check is completely bypassed, allowing any unauthenticated attacker to modify the full CDN configuration — including CDN URLs, storage credentials, and the authentication key itself — via mass-assignment through the `par` request parameter. ## Details The CDN plugin defines a default empty key in `plugin/CDN/CDN.php:68`: ```php $obj->key = ""; ``` The `status.json.php` endpoint authenticates requests using this key, but the check has a critical logic flaw at lines 16-27: ```php // Line 16-19: Requires attacker to provide SOME key value if (empty($_REQUEST['key'])) { $resp->msg = 'Key is empty'; die(json_encode($resp)); } // Line 21-26: Only validates key IF stored key is non-empty if (!empty($obj->key)) { // When key is "" (default), this is FALSE //check the key if ($obj->key !== $_REQUEST['key']) { $resp->msg = 'Key Does not match'; die(json_encode($resp)); } } ``` When the stored key is the default empty string `""`, `!empty("")` evaluates to `false`, and the entire key comparison block is skipped. Any non-empty value provided by the attacker passes authentication. Following the bypass, lines 28-31 perform unchecked mass-assignment: ```php $obj->key = $_REQUE

**Affected:** composer/wwbn/avideo  
**Attack vector:** `path-traversal`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-33719) _(advisory)_
- [GHSA-r64r-883r-wcwh](https://github.com/advisories/GHSA-r64r-883r-wcwh) _(advisory)_
- [https://github.com/WWBN/AVideo/security/advisories/GHSA-r64r-883r-wcwh](https://github.com/WWBN/AVideo/security/advisories/GHSA-r64r-883r-wcwh) _(reference)_
- [https://github.com/WWBN/AVideo/commit/adeff0a31ba04a56f411eef256139fd7ed7d4310](https://github.com/WWBN/AVideo/commit/adeff0a31ba04a56f411eef256139fd7ed7d4310) _(reference)_

**Tags:** `cve`, `ghsa`, `path-traversal`, `wwbn-avideo`

</div>

