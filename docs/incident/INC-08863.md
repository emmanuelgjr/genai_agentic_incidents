---
title: "INC-08863 — WWBN AVideo: Missing CSRF Protection on State-Changing JSON Endpoints Enables Forced Comment Creation, Vote Manipulation, and Category Asset Deletion"
layout: incident
permalink: /incident/INC-08863.html
incident_id: INC-08863
year: 2026
severity: Medium
description_preview: "## Summary Multiple AVideo JSON endpoints under `objects/` accept state-changing requests via `$_REQUEST`/`$_GET` and persist changes tied to the caller's session user, without any anti-CSRF token, or"
tags_str: "cve, ghsa, wwbn-avideo"
---

{% raw %}
<div class="incident-anchor" id="inc-08863" markdown="1">

### INC-08863  [↗](/incident/INC-08863.html)

**WWBN AVideo: Missing CSRF Protection on State-Changing JSON Endpoints Enables Forced Comment Creation, Vote Manipulation, and Category Asset Deletion**  
_2026-04 · vulnerability-disclosure · Severity: Medium_

CVEs: `CVE-2026-40928`
CWEs: `CWE-352`
CVSS: **5.4** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:N/I:L/A:L`

## Summary Multiple AVideo JSON endpoints under `objects/` accept state-changing requests via `$_REQUEST`/`$_GET` and persist changes tied to the caller's session user, without any anti-CSRF token, origin check, or referer check. A malicious page visited by a logged-in victim can silently: 1. Cast/flip the victim's like/dislike on any comment (`objects/comments_like.json.php`). 2. Post a comment authored by the victim on any video, with attacker-chosen text (`objects/commentAddNew.json.php`). 3. Delete assets from any category (`objects/categoryDeleteAssets.json.php`) when the victim has category management rights. Each endpoint is reachable from a browser via a simple `<img src="…">` tag or form submission, so exploitation only requires the victim to load an attacker-controlled HTML resource. ## Details AVideo exposes a helper, `forbidIfIsUntrustedRequest()` (`objects/functionsSecurity.php:138`), that rejects cross-origin requests when the `Referer`/`Origin` does not match `webSiteRootURL`. It is only invoked in one file in the tree — `objects/userUpdate.json.php:18` — and is *not* applied to the endpoints below. There is also an `isGlobalTokenValid()` helper (`objects/functions.php:2313`) intended for CSRF-style token checks; none of the affected endpoints call it. `allowOrigin()` only sets CORS response headers and does not prevent cookie-bearing top-level or image requests from reaching the server. ### 1. `objects/comments_like.json.php` — CSRF → forced like/dislike

**Affected:** composer/wwbn/avideo  
**Attack vector:** `other`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-40928) _(advisory)_
- [GHSA-x2pw-9c38-cp2j](https://github.com/advisories/GHSA-x2pw-9c38-cp2j) _(advisory)_
- [https://github.com/WWBN/AVideo/security/advisories/GHSA-x2pw-9c38-cp2j](https://github.com/WWBN/AVideo/security/advisories/GHSA-x2pw-9c38-cp2j) _(reference)_
- [https://github.com/WWBN/AVideo/commit/7aaad601bd9cd7b993ba0ee1b1bea6c32ee7b77c](https://github.com/WWBN/AVideo/commit/7aaad601bd9cd7b993ba0ee1b1bea6c32ee7b77c) _(reference)_

**Tags:** `cve`, `ghsa`, `wwbn-avideo`

</div>

{% endraw %}
