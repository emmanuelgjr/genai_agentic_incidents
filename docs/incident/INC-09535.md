---
title: "INC-09535 — Veramo is Vulnerable to SQL Injection in Veramo Data Store ORM"
layout: incident
permalink: /incident/INC-09535.html
incident_id: INC-09535
year: 2026
severity: Medium
description_preview: "## Summary An SQL injection vulnerability exists in the `@veramo/data-store` package that allows any authenticated user to execute arbitrary SQL queries against the database. The vulnerability is caus"
tags_str: "ghsa, sql-injection, veramo-data-store"
---

{% raw %}
<div class="incident-anchor" id="inc-09535" markdown="1">

### INC-09535  [↗](/incident/INC-09535.html)

**Veramo is Vulnerable to SQL Injection in Veramo Data Store ORM**  
_2026-01 · vulnerability-disclosure · Severity: Medium_
CWEs: `CWE-89`
CVSS: **6.8** — `CVSS:3.1/AV:N/AC:L/PR:H/UI:N/S:C/C:H/I:N/A:N`

## Summary An SQL injection vulnerability exists in the `@veramo/data-store` package that allows any authenticated user to execute arbitrary SQL queries against the database. The vulnerability is caused by insufficient validation of the `column` parameter in the `order` array of query requests. ## Details `packages/data-store/src/data-store-orm.ts` (lines 416-434) The vulnerability exists in the `decorateQB()` function which processes query ordering parameters: ```typescript function decorateQB( qb: SelectQueryBuilder<any>, tableName: string, input: FindArgs<any>, ): SelectQueryBuilder<any> { if (input?.skip) qb = qb.offset(input.skip) if (input?.take) qb = qb.limit(input.take) if (input?.order) { for (const item of input.order) { qb = qb.addSelect( qb.connection.driver.escape(tableName) + '.' + qb.connection.driver.escape(item.column), item.column, ) qb = qb.orderBy(qb.connection.driver.escape(item.column), item.direction) } } return qb } ``` **Root Cause:** 1. The `item.column` value from user input is passed directly as the **alias** parameter to `addSelect()` without any sanitization or validation 2. While TypeScript defines allowed column types (e.g., `TCredentialColumns = 'context' | 'type' | ...`), this is only compile-time checking 3. At runtime, the function accepts `FindArgs<any>`, allowing arbitrary strings to bypass type restrictions 4. TypeORM inserts the alias directly into the SQL query, enabling

**Affected:** npm/@veramo/data-store  
**Attack vector:** `sql-injection`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`, `AML.T0050`  

**References:**
- [GHSA-38cw-85xc-xr9x](https://github.com/advisories/GHSA-38cw-85xc-xr9x) _(advisory)_
- [https://github.com/decentralized-identity/veramo/security/advisories/GHSA-38cw-85xc-xr9x](https://github.com/decentralized-identity/veramo/security/advisories/GHSA-38cw-85xc-xr9x) _(reference)_
- [https://github.com/decentralized-identity/veramo/pull/1482](https://github.com/decentralized-identity/veramo/pull/1482) _(reference)_
- [https://github.com/decentralized-identity/veramo/commit/067e39dd76f11ee2d25b99c8361d4f02a4223e3b](https://github.com/decentralized-identity/veramo/commit/067e39dd76f11ee2d25b99c8361d4f02a4223e3b) _(reference)_
- [https://github.com/decentralized-identity/veramo/releases/tag/v6.0.2](https://github.com/decentralized-identity/veramo/releases/tag/v6.0.2) _(reference)_

**Tags:** `ghsa`, `sql-injection`, `veramo-data-store`

</div>

{% endraw %}
