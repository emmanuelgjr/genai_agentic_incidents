---
title: "INC-09978 — Libredesk has Improper Neutralization of HTML Tags in a Web Page"
layout: incident
permalink: /incident/INC-09978.html
incident_id: INC-09978
year: 2025
severity: High
description_preview: "### Summary LibreDesk is vulnerable to **stored HTML injection** in the contact notes feature. When adding notes via `POST /api/v1/contacts/{id}/notes`, the backend automatically wraps user input in `"
tags_str: "cve, ghsa, github.com-abhinavxd-libredesk"
---

{% raw %}
<div class="incident-anchor" id="inc-09978" markdown="1">

### INC-09978  [↗](/incident/INC-09978.html)

**Libredesk has Improper Neutralization of HTML Tags in a Web Page**  
_2025-12 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2025-68927`
CWEs: `CWE-79`

### Summary LibreDesk is vulnerable to **stored HTML injection** in the contact notes feature. When adding notes via `POST /api/v1/contacts/{id}/notes`, the backend automatically wraps user input in `<p>` tags. However, by intercepting the request and removing the `<p>` tag, an attacker can inject arbitrary HTML elements such as forms and images, which are then stored and rendered without proper sanitization. This can lead to phishing, CSRF-style forced actions, and UI redress attacks. --- ### Details When notes are added through the LibreDesk web application, the client sends note content wrapped inside `<p>` tags. The backend appears to **trust this HTML structure** and stores the content as-is. By intercepting the request to: ``` POST /api/v1/contacts/3/notes ``` and **removing the `<p>` wrapper**, an attacker can submit arbitrary HTML content. The backend does not sanitize or validate the HTML payload before persisting it. As a result: * Arbitrary HTML tags (e.g., `<form>`, `<input>`, `<img>`) are stored * The injected HTML is rendered when the notes are viewed in the application * No server-side HTML sanitization or allowlisting is enforced This indicates that the application relies on **client-side HTML formatting assumptions**, which can be bypassed by modifying the request. --- ### PoC 1. Log in to LibreDesk and open any contact. 2. Add a note normally via the UI. 3. Intercept the request to: ``` POST /api/v1/contacts/3/notes ``` 4. Original req

**Affected:** go/github.com/abhinavxd/libredesk  
**Attack vector:** `phishing`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2025-68927) _(advisory)_
- [GHSA-wh6m-h6f4-rjf4](https://github.com/advisories/GHSA-wh6m-h6f4-rjf4) _(advisory)_
- [https://github.com/abhinavxd/libredesk/security/advisories/GHSA-wh6m-h6f4-rjf4](https://github.com/abhinavxd/libredesk/security/advisories/GHSA-wh6m-h6f4-rjf4) _(reference)_
- [https://github.com/abhinavxd/libredesk/commit/270347849943ac6a43e9fd6ebdc99c71841900eb](https://github.com/abhinavxd/libredesk/commit/270347849943ac6a43e9fd6ebdc99c71841900eb) _(reference)_

**Tags:** `cve`, `ghsa`, `github.com-abhinavxd-libredesk`

</div>

{% endraw %}
