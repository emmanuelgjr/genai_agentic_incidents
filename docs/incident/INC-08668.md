---
title: "INC-08668 — Kimai vulnerable to formula Injection via tag names in XLSX export"
layout: incident
permalink: /incident/INC-08668.html
incident_id: INC-08668
year: 2026
severity: Medium
description_preview: "## Summary Any `ROLE_USER` can create a tag with a formula string as its name (e.g. `=SUM(54+51)`) via `POST /api/tags` and assign it to a timesheet. When an admin exports timesheets to XLSX, `ArrayFo"
tags_str: "cve, ghsa, kimai-kimai"
---

<div class="incident-anchor" id="inc-08668" markdown="1">

### INC-08668  [↗](/incident/INC-08668.html)

**Kimai vulnerable to formula Injection via tag names in XLSX export**  
_2026-05 · vulnerability-disclosure · Severity: Medium_

CVEs: `CVE-2026-41498`, `CVE-2026-42267`
CWEs: `CWE-1236`, `CWE-862`
CVSS: **5.7** — `CVSS:3.1/AV:N/AC:L/PR:L/UI:R/S:U/C:N/I:H/A:N`

## Summary Any `ROLE_USER` can create a tag with a formula string as its name (e.g. `=SUM(54+51)`) via `POST /api/tags` and assign it to a timesheet. When an admin exports timesheets to XLSX, `ArrayFormatter.formatValue()` joins tag names with `implode()` and returns the result unchanged. OpenSpout promotes any `=`-prefixed string to a `FormulaCell`, writing `<f>SUM(54+51)</f>` into the XLSX archive. Excel evaluates the formula when the file is opened. ## Details ### 1. `ArrayFormatter` does not sanitize before returning `sanitizeDDE()` exists on `StringHelper` and is called by `TextFormatter`, but `ArrayFormatter` never calls it. ```php // src/Export/Package/CellFormatter/ArrayFormatter.php:24 return implode(', ', $value); // no sanitizeDDE() call ``` ### 2. Tag name validation does not block formula trigger characters The API blocks commas in tag names but permits `=`, `+`, `-`, and `@` - all valid formula prefixes in Excel and LibreOffice Calc. ### 3. OpenSpout silently promotes strings to formula cells `Cell::fromValue("=SUM(54+51)")` returns a `FormulaCell` with no warning. ### PoC 1. It logs in as normal user, creates tag `=SUM(54+51)`, assigns it to a timesheet. 2. Admin has to export timesheets to Excel version via `/en/export/` endpoint. <img width="1339" height="700" alt="image" src="https://github.com/user-attachments/assets/884c7943-5e3b-4647-8bcc-e264d6719d66" /> <img width="1304" height="128" alt="formula_injection_tags" src="https://github.com/user

**Affected:** composer/kimai/kimai  
**Attack vector:** `other`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI04`  
**NIST AI RMF:** `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-42267) _(advisory)_
- [GHSA-3xc2-h5r3-wv3r](https://github.com/advisories/GHSA-3xc2-h5r3-wv3r) _(advisory)_
- [https://github.com/kimai/kimai/security/advisories/GHSA-3xc2-h5r3-wv3r](https://github.com/kimai/kimai/security/advisories/GHSA-3xc2-h5r3-wv3r) _(reference)_
- [https://github.com/kimai/kimai/releases/tag/2.54.0](https://github.com/kimai/kimai/releases/tag/2.54.0) _(reference)_
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-41498) _(advisory)_
- [GHSA-jv9x-w4gm-hwcm](https://github.com/advisories/GHSA-jv9x-w4gm-hwcm) _(advisory)_
- [https://github.com/kimai/kimai/security/advisories/GHSA-jv9x-w4gm-hwcm](https://github.com/kimai/kimai/security/advisories/GHSA-jv9x-w4gm-hwcm) _(reference)_

**Tags:** `cve`, `ghsa`, `kimai-kimai`

</div>

