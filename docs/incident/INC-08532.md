---
title: "INC-08532 — @apostrophecms/cli: Command Injection in apos create via Unsanitized Password Input"
layout: incident
permalink: /incident/INC-08532.html
incident_id: INC-08532
year: 2026
severity: Medium
description_preview: "Summary The @apostrophecms/cli package contains a command injection vulnerability in the apos create command. User-supplied input from the password prompt is embedded directly into a shell command wit"
tags_str: "apostrophecms-cli, command-injection, cve, ghsa"
---

{% raw %}
<div class="incident-anchor" id="inc-08532" markdown="1">

### INC-08532  [↗](/incident/INC-08532.html)

**@apostrophecms/cli: Command Injection in apos create via Unsanitized Password Input**  
_2026-05 · vulnerability-disclosure · Severity: Medium_

CVEs: `CVE-2026-42853`
CWEs: `CWE-78`
CVSS: **6.5** — `CVSS:3.1/AV:L/AC:L/PR:H/UI:R/S:U/C:H/I:H/A:H`

Summary The @apostrophecms/cli package contains a command injection vulnerability in the apos create command. User-supplied input from the password prompt is embedded directly into a shell command without proper sanitization or escaping. This allows execution of arbitrary commands on the host system. ━━━━━━━━━━━━━━━━━━━━━━ Details Vulnerable file: lib/commands/create.js Location: Line 186 The CLI collects a password using an interactive prompt and passes it directly into a shell command. Vulnerable code: const response = await prompts({ type: 'password', name: 'pw', message: '🔏 Please enter a password:' }); exec(echo "${response.pw}" | ${createUserCommand}); The value of response.pw is not validated, sanitized, or escaped before being used in exec(). This allows shell metacharacters such as ;, &&, and $() to break out of the intended command and execute arbitrary commands. ━━━━━━━━━━━━━━━━━━━━━━ Steps to Reproduce 1) Install the CLI npm install -g @apostrophecms/cli 2) Create a new project mkdir testproject && cd testproject apos create mysite 3)When prompted for the admin password, enter "; id > /tmp/apos_rce_proof.txt; echo " 4)Verify command execution cat /tmp/apos_rce_proof.txt ━━━━━━━━━━━━━━━━━━━━━━ Proof of Concept Output uid=1000(vboxuser) gid=1000(vboxuser) groups=1000(vboxuser),27(sudo),984(docker) This confirms arbitrary command execution with the privileges of the user running the CLI. ━━━━━━━━━━━━━━━━━━━━━━ Impact

**Affected:** npm/@apostrophecms/cli  
**Attack vector:** `command-injection`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-42853) _(advisory)_
- [GHSA-hcwq-x9fw-8cfq](https://github.com/advisories/GHSA-hcwq-x9fw-8cfq) _(advisory)_
- [https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-hcwq-x9fw-8cfq](https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-hcwq-x9fw-8cfq) _(reference)_

**Tags:** `apostrophecms-cli`, `command-injection`, `cve`, `ghsa`

</div>

{% endraw %}
