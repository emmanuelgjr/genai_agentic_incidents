---
title: "INC-08872 — goshs has CSRF in state-changing GET routes enables authenticated file deletion and directory creation"
layout: incident
permalink: /incident/INC-08872.html
incident_id: INC-08872
year: 2026
severity: Medium
description_preview: "### Summary goshs contains a cross-site request forgery issue in its state-changing HTTP GET routes. An external attacker can cause an already authenticated browser to trigger destructive actions such"
tags_str: "cve, ghsa, github.com-patrickhener-goshs-v2"
---

<div class="incident-anchor" id="inc-08872" markdown="1">

### INC-08872  [↗](/incident/INC-08872.html)

**goshs has CSRF in state-changing GET routes enables authenticated file deletion and directory creation**  
_2026-04 · vulnerability-disclosure · Severity: Medium_

CVEs: `CVE-2026-40883`
CWEs: `CWE-352`
CVSS: **8.1** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:N/I:H/A:H`

### Summary goshs contains a cross-site request forgery issue in its state-changing HTTP GET routes. An external attacker can cause an already authenticated browser to trigger destructive actions such as `?delete` and `?mkdir` because goshs relies on HTTP basic auth alone and performs no CSRF, `Origin`, or `Referer` validation for those routes. I reproduced this on `v2.0.0-beta.5`. ### Details The vulnerable request handling is reachable through normal GET requests: - `httpserver/handler.go:118-123` dispatches `?mkdir` directly to `handleMkdir()` - `httpserver/handler.go:180-186` dispatches `?delete` directly to `deleteFile()` Authentication is enforced only by HTTP basic auth: - `httpserver/middleware.go:20-87` accepts any request that presents valid cached or replayed basic-auth credentials The resulting state changes hit filesystem mutation sinks: - `httpserver/handler.go:683-718` calls `os.RemoveAll()` in `deleteFile()` - `httpserver/handler.go:961-1000` calls `os.MkdirAll()` in `handleMkdir()` Because browsers can replay HTTP basic-auth credentials on subresource requests, an attacker-controlled page can embed: - `<img src="http://127.0.0.1:18095/victim.txt?delete">` - `<img src="http://127.0.0.1:18095/csrfmade?mkdir">` If the victim has already authenticated to goshs, those requests are treated as legitimate authenticated actions and the server mutates the filesystem. ### PoC Manual verification commands used: `Terminal 1` ```bash cd '/Users/r1zzg0d/Document

**Affected:** go/github.com/patrickhener/goshs/v2  
**Attack vector:** `other`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-40883) _(advisory)_
- [GHSA-jrq5-hg6x-j6g3](https://github.com/advisories/GHSA-jrq5-hg6x-j6g3) _(advisory)_
- [https://github.com/patrickhener/goshs/security/advisories/GHSA-jrq5-hg6x-j6g3](https://github.com/patrickhener/goshs/security/advisories/GHSA-jrq5-hg6x-j6g3) _(reference)_

**Tags:** `cve`, `ghsa`, `github.com-patrickhener-goshs-v2`

</div>

