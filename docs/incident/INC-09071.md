---
title: "INC-09071 — Happy DOM ECMAScriptModuleCompiler: unsanitized export names are interpolated as executable code"
layout: incident
permalink: /incident/INC-09071.html
incident_id: INC-09071
year: 2026
severity: High
description_preview: "### Summary A code injection vulnerability in `ECMAScriptModuleCompiler` allows an attacker to achieve Remote Code Execution (RCE) by injecting arbitrary JavaScript expressions inside `export { }` dec"
tags_str: "cve, ghsa, happy-dom, rce"
---

<div class="incident-anchor" id="inc-09071" markdown="1">

### INC-09071  [↗](/incident/INC-09071.html)

**Happy DOM ECMAScriptModuleCompiler: unsanitized export names are interpolated as executable code**  
_2026-03 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-33943`
CWEs: `CWE-94`
CVSS: **8.8** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:U/C:H/I:H/A:H`

### Summary A code injection vulnerability in `ECMAScriptModuleCompiler` allows an attacker to achieve Remote Code Execution (RCE) by injecting arbitrary JavaScript expressions inside `export { }` declarations in ES module scripts processed by happy-dom. The compiler directly interpolates unsanitized content into generated code as an executable expression, and the quote filter does not strip backticks, allowing template literal-based payloads to bypass sanitization. ### Details **Vulnerable file**: `packages/happy-dom/src/module/ECMAScriptModuleCompiler.ts`, lines 371-385 The "Export object" handler extracts content from `export { ... }` using the regex `export\s*{([^}]+)}`, then generates executable code by directly interpolating it: } else if (match[16] && isTopLevel && PRECEDING_STATEMENT_TOKEN_REGEXP.test(precedingToken)) { // Export object const parts = this.removeMultilineComments(match[16]).split(/\s*,\s*/); const exportCode: string[] = []; for (const part of parts) { const nameParts = part.trim().split(/\s+as\s+/); const exportName = (nameParts[1] || nameParts[0]).replace(/["']/g, ''); const importName = nameParts[0].replace(/["']/g, ''); // backticks NOT stripped if (exportName && importName) { exportCode.push(`$happy_dom.exports['${exportName}'] = ${importName}`); // importName is inserted as executable code, not as a string

**Affected:** npm/happy-dom  
**Attack vector:** `rce`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-33943) _(advisory)_
- [GHSA-6q6h-j7hj-3r64](https://github.com/advisories/GHSA-6q6h-j7hj-3r64) _(advisory)_
- [https://github.com/capricorn86/happy-dom/security/advisories/GHSA-6q6h-j7hj-3r64](https://github.com/capricorn86/happy-dom/security/advisories/GHSA-6q6h-j7hj-3r64) _(reference)_
- [https://github.com/capricorn86/happy-dom/commit/5437fdf8f13adb9590f9f52616d9f69c3ee8db3c](https://github.com/capricorn86/happy-dom/commit/5437fdf8f13adb9590f9f52616d9f69c3ee8db3c) _(reference)_
- [https://github.com/capricorn86/happy-dom/releases/tag/v20.8.8](https://github.com/capricorn86/happy-dom/releases/tag/v20.8.8) _(reference)_

**Tags:** `cve`, `ghsa`, `happy-dom`, `rce`

</div>

