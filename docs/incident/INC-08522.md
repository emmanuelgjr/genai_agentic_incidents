---
title: "INC-08522 — Apostrophe has default XSS via `xmp` raw-text passthrough in `sanitize-html`"
layout: incident
permalink: /incident/INC-08522.html
incident_id: INC-08522
year: 2026
severity: Critical
description_preview: "### Summary Under the default configuration, `sanitize-html` can turn attacker-controlled content inside a disallowed `xmp` element into live HTML or JavaScript. This is a sanitizer bypass in the defa"
tags_str: "cve, ghsa, sanitize-html, xss"
---

{% raw %}
<div class="incident-anchor" id="inc-08522" markdown="1">

### INC-08522  [↗](/incident/INC-08522.html)

**Apostrophe has default XSS via `xmp` raw-text passthrough in `sanitize-html`**  
_2026-05 · vulnerability-disclosure · Severity: Critical_

CVEs: `CVE-2026-44990`
CWEs: `CWE-79`
CVSS: **9.3** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:R/S:C/C:H/I:H/A:N`

### Summary Under the default configuration, `sanitize-html` can turn attacker-controlled content inside a disallowed `xmp` element into live HTML or JavaScript. This is a sanitizer bypass in the default `disallowedTagsMode: 'discard'` path and can lead to stored XSS in applications that render sanitized output back to users. ### Details In `sanitize-html@2.17.3`, the default `nonTextTags` list includes only `script`, `style`, `textarea`, and `option` in `index.js` lines 138-142. That means disallowed `xmp` tags are not treated as "drop the entire contents" tags. Later, in the `ontext` handler at `index.js` lines 569-577, the code special-cases `textarea` and `xmp` and appends their text content directly to the output without escaping: ```js } else if ((options.disallowedTagsMode === 'discard' || options.disallowedTagsMode === 'completelyDiscard') && (tag === 'textarea' || tag === 'xmp')) { result += text; } ``` Because `htmlparser2` treats `xmp` as a raw-text element, markup inside `xmp` is parsed as text on input but becomes live markup again once it is appended unescaped to the sanitized output. This creates a default sanitizer bypass. For example, a disallowed `<xmp>` wrapper can be used to smuggle `<script>` or event-handler payloads through sanitization. The README also appears to contradict the implementation. In the "Discarding the entire contents of a disallowed tag" section, the documented exception list names only `style`, `script`, `textarea`, and `option`

**Affected:** npm/sanitize-html  
**Attack vector:** `xss`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-44990) _(advisory)_
- [GHSA-rpr9-rxv7-x643](https://github.com/advisories/GHSA-rpr9-rxv7-x643) _(advisory)_
- [https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-rpr9-rxv7-x643](https://github.com/apostrophecms/apostrophe/security/advisories/GHSA-rpr9-rxv7-x643) _(reference)_
- [https://github.com/apostrophecms/apostrophe/issues/5418](https://github.com/apostrophecms/apostrophe/issues/5418) _(reference)_
- [https://github.com/apostrophecms/apostrophe/commit/8d4c882b4ed3a7ce802cd87f89f0c1cb7482b8c2](https://github.com/apostrophecms/apostrophe/commit/8d4c882b4ed3a7ce802cd87f89f0c1cb7482b8c2) _(reference)_

**Tags:** `cve`, `ghsa`, `sanitize-html`, `xss`

</div>

{% endraw %}
