---
title: "INC-08630 — fast-jwt: JWT auth bypass due to empty HMAC secret accepted by async key resolver"
layout: incident
permalink: /incident/INC-08630.html
incident_id: INC-08630
year: 2026
severity: Critical
description_preview: "### Summary A critical authentication-bypass vulnerability in `fast-jwt`'s async key-resolver flow allows any unauthenticated attacker to forge arbitrary JWTs that are accepted as authentic. When the"
tags_str: "cve, fast-jwt, ghsa"
---

<div class="incident-anchor" id="inc-08630" markdown="1">

### INC-08630  [↗](/incident/INC-08630.html)

**fast-jwt: JWT auth bypass due to empty HMAC secret accepted by async key resolver**  
_2026-05 · vulnerability-disclosure · Severity: Critical_

CVEs: `CVE-2026-44351`
CWEs: `CWE-1391`, `CWE-287`, `CWE-326`
CVSS: **9.1** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:H/I:H/A:N`

### Summary A critical authentication-bypass vulnerability in `fast-jwt`'s async key-resolver flow allows any unauthenticated attacker to forge arbitrary JWTs that are accepted as authentic. When the application's key resolver returns an empty string (`''`), for example via the common `keys[decoded.header.kid] || ''` JWKS-style fallback, fast-jwt converts it to a zero-length `Buffer`, hands it to `crypto.createSecretKey`, derives `allowedAlgorithms = ['HS256','HS384','HS512']` from it, and then verifies the token's signature against an empty-key HMAC. The attacker simply computes `HMAC-SHA256(key='', input='${header}.${payload}')`, which Node accepts without complaint — and the verifier returns the attacker-chosen payload (sub, admin, scopes, etc.) as authentic. Reproducible 100% against the current latest release `fast-jwt@6.2.3`. ### Preconditions For this issue to occur the following MUST ALL be true: 1. The application developer (library consumer) uses an asynchronous callback function to set the key (e.g. `createVerifier({key: async (decoded) => ... })`) 2. The response from the async callback MUST return an empty string `''` OR zero-length buffer (e.g. `Buffer.alloc(0)`). Any other empty/missing return values (e.g. null, undefined) do not trigger this issue 3. The library configuration must allow HMAC signatures. This is the default for the library. 4. The bad actor MUST have signed their token with an empty string. This is a trivial task and requires no special kno

**Affected:** npm/fast-jwt  
**Attack vector:** `auth-bypass`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-44351) _(advisory)_
- [GHSA-gmvf-9v4p-v8jc](https://github.com/advisories/GHSA-gmvf-9v4p-v8jc) _(advisory)_
- [https://github.com/nearform/fast-jwt/security/advisories/GHSA-gmvf-9v4p-v8jc](https://github.com/nearform/fast-jwt/security/advisories/GHSA-gmvf-9v4p-v8jc) _(reference)_

**Tags:** `cve`, `fast-jwt`, `ghsa`

</div>

