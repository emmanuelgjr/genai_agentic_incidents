---
title: "INC-09238 — SiYuan has a SVG Sanitizer Bypass via Whitespace in `javascript:` URI — Unauthenticated XSS"
layout: incident
permalink: /incident/INC-09238.html
incident_id: INC-09238
year: 2026
severity: Medium
description_preview: "# SVG Sanitizer Bypass via Whitespace in `javascript:` URI — Unauthenticated XSS ## Summary SiYuan's SVG sanitizer (`SanitizeSVG`) checks `href` attributes for the `javascript:` prefix using `strings."
tags_str: "cve, ghsa, github.com-siyuan-note-siyuan-kernel, xss"
---

{% raw %}
<div class="incident-anchor" id="inc-09238" markdown="1">

### INC-09238  [↗](/incident/INC-09238.html)

**SiYuan has a SVG Sanitizer Bypass via Whitespace in `javascript:` URI — Unauthenticated XSS**  
_2026-03 · vulnerability-disclosure · Severity: Medium_

CVEs: `CVE-2026-31807`, `CVE-2026-31809`
CWEs: `CWE-79`

# SVG Sanitizer Bypass via Whitespace in `javascript:` URI — Unauthenticated XSS ## Summary SiYuan's SVG sanitizer (`SanitizeSVG`) checks `href` attributes for the `javascript:` prefix using `strings.HasPrefix()`. However, inserting ASCII tab (`&#9;`), newline (`&#10;`), or carriage return (`&#13;`) characters inside the `javascript:` string bypasses this prefix check. Browsers strip these characters per the WHATWG URL specification before parsing the URL scheme, so the JavaScript still executes. This allows an attacker to inject executable JavaScript into the unauthenticated `/api/icon/getDynamicIcon` endpoint, creating a reflected XSS. This is a second bypass of the fix for CVE-2026-29183 (fixed in v3.5.9), [distinct from the `<animate>` element bypass](https://github.com/siyuan-note/siyuan/security/advisories/GHSA-5hc8-qmg8-pw27). ## Affected Component - **File:** `kernel/util/misc.go` - **Function:** `SanitizeSVG()` (lines 234-319) - **Specific check:** Line 271 — `strings.HasPrefix(val, "javascript:")` - **Endpoint:** `GET /api/icon/getDynamicIcon?type=8&content=...` (unauthenticated) - **Version:** SiYuan <= 3.5.9 ## Root Cause The sanitizer uses Go's `html.Parse` which decodes HTML entities in attribute values. When the input contains `java&#9;script:alert(1)`, the parser decodes `&#9;` to a literal tab character (U+0009). The sanitizer then checks: ```go val := strings.TrimSpace(strings.ToLower(a.Val)) // val is now "java\tscript:alert(1)" if strings.HasPrefi

**Affected:** go/github.com/siyuan-note/siyuan/kernel  
**Attack vector:** `xss`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-31809) _(advisory)_
- [GHSA-pmc9-f5qr-2pcr](https://github.com/advisories/GHSA-pmc9-f5qr-2pcr) _(advisory)_
- [https://github.com/siyuan-note/siyuan/security/advisories/GHSA-pmc9-f5qr-2pcr](https://github.com/siyuan-note/siyuan/security/advisories/GHSA-pmc9-f5qr-2pcr) _(reference)_
- [https://github.com/siyuan-note/siyuan/releases/tag/v3.5.10](https://github.com/siyuan-note/siyuan/releases/tag/v3.5.10) _(reference)_
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-31807) _(advisory)_
- [GHSA-5hc8-qmg8-pw27](https://github.com/advisories/GHSA-5hc8-qmg8-pw27) _(advisory)_
- [https://github.com/siyuan-note/siyuan/security/advisories/GHSA-5hc8-qmg8-pw27](https://github.com/siyuan-note/siyuan/security/advisories/GHSA-5hc8-qmg8-pw27) _(reference)_

**Tags:** `cve`, `ghsa`, `github.com-siyuan-note-siyuan-kernel`, `xss`

</div>

{% endraw %}
