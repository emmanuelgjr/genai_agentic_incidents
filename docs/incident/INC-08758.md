---
title: "INC-08758 — russh has pre-auth DoS via unbounded allocation in its keyboard-interactive auth handler"
layout: incident
permalink: /incident/INC-08758.html
incident_id: INC-08758
year: 2026
severity: High
description_preview: "## Summary A pre-authentication denial-of-service vulnerability exists in the server's keyboard-interactive authentication handler. A malicious client can crash any russh-based server that implements"
tags_str: "cve, ghsa, russh"
---

{% raw %}
<div class="incident-anchor" id="inc-08758" markdown="1">

### INC-08758  [↗](/incident/INC-08758.html)

**russh has pre-auth DoS via unbounded allocation in its keyboard-interactive auth handler**  
_2026-04 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-42189`
CWEs: `CWE-770`, `CWE-789`
CVSS: **7.5** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:H`

## Summary A pre-authentication denial-of-service vulnerability exists in the server's keyboard-interactive authentication handler. A malicious client can crash any russh-based server that implements keyboard-interactive auth (e.g., for 2FA/TOTP) with a single malformed packet, requiring no credentials. ## Vulnerability Details In `russh/src/server/encrypted.rs`, the function `read_userauth_info_response` decodes a `u32` count from the client's `SSH_MSG_USERAUTH_INFO_RESPONSE` and passes it directly to `Vec::with_capacity()`: ```rust let n = map_err!(u32::decode(r))?; // Bound both allocation and iteration by remaining packet data to // prevent a malicious client from causing a multi-GB allocation or // billions of loop iterations with a crafted count. // Each response needs at least 4 bytes (length prefix). let max_responses = r.remaining_len().saturating_add(3) / 4; let n = (n as usize).min(max_responses); let mut responses = Vec::with_capacity(n); for _ in 0..n { responses.push(Bytes::decode(r).ok()) } ``` An attacker can send `n = 0x10000000` (268M) or larger in a minimal packet (~50 bytes after encryption). The server attempts to allocate `n * ~24 bytes` (size of `Option<Bytes>`) = ~6.4GB, causing an OOM crash. ## Attack Flow 1. Attacker connects via TCP, completes key exchange (no credentials needed -- this is the anonymous DH handshake, not authentication) 2. Sends `USERAUTH_REQUEST` with method `keyboard-interactive` 3. Server handler returns `Auth::Partia

**Affected:** rust/russh  
**Attack vector:** `dos`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-42189) _(advisory)_
- [GHSA-f5v4-2wr6-hqmg](https://github.com/advisories/GHSA-f5v4-2wr6-hqmg) _(advisory)_
- [https://github.com/Eugeny/russh/security/advisories/GHSA-f5v4-2wr6-hqmg](https://github.com/Eugeny/russh/security/advisories/GHSA-f5v4-2wr6-hqmg) _(reference)_
- [https://github.com/Eugeny/russh/commit/6c3c80a9b6d60763d6227d60fa8310e57172a4d1](https://github.com/Eugeny/russh/commit/6c3c80a9b6d60763d6227d60fa8310e57172a4d1) _(reference)_
- [https://github.com/Eugeny/russh/releases/tag/v0.60.1](https://github.com/Eugeny/russh/releases/tag/v0.60.1) _(reference)_

**Tags:** `cve`, `ghsa`, `russh`

</div>

{% endraw %}
