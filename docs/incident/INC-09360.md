---
title: "INC-09360 — esm.sh has SSRF localhost/private-network bypass in `/http(s)` module route"
layout: incident
permalink: /incident/INC-09360.html
incident_id: INC-09360
year: 2026
severity: High
description_preview: "### Summary An SSRF vulnerability (CWE-918) exists in esm.sh’s `/http(s)` fetch route. The service tries to block localhost/internal targets, but the validation is based on hostname string checks and"
tags_str: "cve, ghsa, github.com-esm-dev-esm.sh, ssrf"
---

{% raw %}
<div class="incident-anchor" id="inc-09360" markdown="1">

### INC-09360  [↗](/incident/INC-09360.html)

**esm.sh has SSRF localhost/private-network bypass in `/http(s)` module route**  
_2026-02 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2026-27730`
CWEs: `CWE-918`
CVSS: **8.6** — `CVSS:3.0/AV:N/AC:L/PR:N/UI:N/S:C/C:H/I:N/A:N`

### Summary An SSRF vulnerability (CWE-918) exists in esm.sh’s `/http(s)` fetch route. The service tries to block localhost/internal targets, but the validation is based on hostname string checks and can be bypassed using DNS alias domains (for example, `127.0.0.1.nip.io` resolving to `127.0.0.1`). This allows an external requester to make the esm.sh server fetch internal localhost services. Severity: High (depending on deployment network exposure). ### Details The vulnerable flow starts at the route handling user-controlled remote URLs: - `server/router.go:532` - Accepts paths beginning with `/http://` or `/https://`. ```go if strings.HasPrefix(pathname, "/http://") || strings.HasPrefix(pathname, "/https://") { query := ctx.Query() modUrl, err := url.Parse(pathname[1:]) if err != nil { ctx.SetHeader("Cache-Control", ccImmutable) return rex.Status(400, "Invalid URL") } if modUrl.Scheme != "http" && modUrl.Scheme != "https" { ctx.SetHeader("Cache-Control", ccImmutable) return rex.Status(400, "Invalid URL") } modUrlStr := modUrl.String() // disallow localhost or ip address for production if !DEBUG { hostname := modUrl.Hostname() if isLocalhost(hostname) || !valid.IsDomain(hostname) || modUrl.Host == ctx.R.Host { ctx.SetHeader("Cache-Control", ccImmutable) return rex.Status(400, "Invalid URL") } } ``` The internal-target block is string-based: - `server/router.go:545` ```go // disallow localhost or ip address for production if

**Affected:** go/github.com/esm-dev/esm.sh  
**Attack vector:** `ssrf`  

**OWASP LLM Top 10:** `LLM03`, `LLM05`  
**OWASP Agentic (ASI):** `ASI03`, `ASI04`, `ASI05`  
**NIST AI RMF:** `GOVERN-1.4`, `GOVERN-6.1`, `MEASURE-2.7`  
**MITRE ATLAS:** `AML.T0010`, `AML.T0012`, `AML.T0049`, `AML.T0050`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2026-27730) _(advisory)_
- [GHSA-p2v6-84h2-5x4r](https://github.com/advisories/GHSA-p2v6-84h2-5x4r) _(advisory)_
- [https://github.com/esm-dev/esm.sh/security/advisories/GHSA-p2v6-84h2-5x4r](https://github.com/esm-dev/esm.sh/security/advisories/GHSA-p2v6-84h2-5x4r) _(reference)_
- [https://github.com/esm-dev/esm.sh/pull/1149](https://github.com/esm-dev/esm.sh/pull/1149) _(reference)_
- [https://github.com/esm-dev/esm.sh/commit/0593516c4cfab49ad3b4900416a8432ff2e23eb0](https://github.com/esm-dev/esm.sh/commit/0593516c4cfab49ad3b4900416a8432ff2e23eb0) _(reference)_
- [https://github.com/esm-dev/esm.sh/releases/tag/v137](https://github.com/esm-dev/esm.sh/releases/tag/v137) _(reference)_

**Tags:** `cve`, `ghsa`, `github.com-esm-dev-esm.sh`, `ssrf`

</div>

{% endraw %}
