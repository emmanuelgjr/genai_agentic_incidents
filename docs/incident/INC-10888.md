---
title: "INC-10888 — Hyperledger Indy's update process of a DID does not check who signs the request"
layout: incident
permalink: /incident/INC-10888.html
incident_id: INC-10888
year: 2024
severity: High
description_preview: "# Name Updating a DID with a nym transaction will be written to the ledger if neither ROLE or VERKEY are being changed, regardless of sender. # Description A malicious DID with no particular role can"
tags_str: "cve, ghsa, indy-node"
---

{% raw %}
<div class="incident-anchor" id="inc-10888" markdown="1">

### INC-10888  [↗](/incident/INC-10888.html)

**Hyperledger Indy's update process of a DID does not check who signs the request**  
_2024-08 · vulnerability-disclosure · Severity: High_

CVEs: `CVE-2020-11093`
CWEs: `CWE-347`
CVSS: **7.5** — `CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:H/A:N`

# Name Updating a DID with a nym transaction will be written to the ledger if neither ROLE or VERKEY are being changed, regardless of sender. # Description A malicious DID with no particular role can ask an update for another DID (but cannot modify its verkey or role). This is bad because: 1. Any DID can write a nym transaction to the ledger (i.e., any DID can spam the ledger with nym transactions). 1. Any DID can change any other DID's alias. 1. The update transaction modifies the ledger metadata associated with a DID. # Expected vs Observed We expect that if a DID (with no role) wants to update another DID (not its own or one it is the endorser), then the nodes should refuse the request. We can see that requirements in the [Indy default auth_rules](https://github.com/hyperledger/indy-node/blob/master/docs/source/auth_rules.md) in Section "Who is the owner" in the last point of "Endorser using". We observe that with a normal DID, we can update the field `from` for a random DID, for example, the one of a TRUSTEE. It creates then a new transaction on the ledger. # Explanation of the attack We first begin to connect to the pool and open a wallet. Then, we will use a TRUSTEE (but can also be a STEWARD or an ENDORSER) DID `V4SGRU86Z58d6TV7PBUe6f`. We ask the information about `V4SGRU86Z58d6TV7PBUe6f` with a get-nym. We create a new DID `V4SGRU86Z58d6TV7PBUe1a` signed by `V4SGRU86Z58d6TV7PBUe6f` with no role. For the rest of the attack, we will use `V4SGRU86Z58d6TV7PBUe1a`

**Affected:** pip/indy-node  
**Attack vector:** `other`  

**OWASP LLM Top 10:** `LLM03`  
**OWASP Agentic (ASI):** `ASI04`  
**NIST AI RMF:** `GOVERN-6.1`  
**MITRE ATLAS:** `AML.T0010`  

**References:**
- [NVD](https://nvd.nist.gov/vuln/detail/CVE-2020-11093) _(advisory)_
- [GHSA-wh2w-39f4-rpv2](https://github.com/advisories/GHSA-wh2w-39f4-rpv2) _(advisory)_
- [https://github.com/hyperledger/indy-node/security/advisories/GHSA-wh2w-39f4-rpv2](https://github.com/hyperledger/indy-node/security/advisories/GHSA-wh2w-39f4-rpv2) _(reference)_
- [https://github.com/hyperledger/indy-node/commit/55056f22c83b7c3520488b615e1577e0f895d75a](https://github.com/hyperledger/indy-node/commit/55056f22c83b7c3520488b615e1577e0f895d75a) _(reference)_
- [https://github.com/hyperledger/indy-node/blob/master/CHANGELOG.md#1124](https://github.com/hyperledger/indy-node/blob/master/CHANGELOG.md#1124) _(reference)_
- [https://github.com/hyperledger/indy-node/blob/master/docs/source/auth_rules.md](https://github.com/hyperledger/indy-node/blob/master/docs/source/auth_rules.md) _(reference)_

**Tags:** `cve`, `ghsa`, `indy-node`

</div>

{% endraw %}
